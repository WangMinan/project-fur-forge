# T51-F8 工程记录（2026-08-14）

## 结论

`PASS WITH FOLLOW-UP`。T51-F8 的本地工程实现和相关自动化门禁已完成；本记录不代签 T49-R1 独立 Review 或用户验收，也不表示已提交、推送、发布或部署。

## 基线与授权

- 开工基线：本地 `main`、upstream 与 `origin/main` 均为 `96da91420638f155e5961d90f4eda4a7a1616d51`；写入前已执行 `git fetch origin` 并确认无远端并发推进。
- 用户明确授权本轮直接在 `main` 实施，不创建分支或 PR；用户未授权提交、推送或发布。
- 范围只包括全局水印管理心智模型、作品端可选预览、精选排序及首页前 12；不增加迁移、唯一索引、拖拽依赖、关闭开关或水印位置选项。

## 实现事实

- `/admin/site/branding` 删除真实预览和预览门槛，改为 Logo 候选、水印参数、保存与刷新三段单列纵排；首次使用“保存并启用水印”，后续使用“保存并刷新全站”。
- `PUT /api/admin/v1/site/branding/watermark` 在一次请求中校验 branding 版本、创建或复用 profile 并启动持久 operation；空库直接激活，有作品时生成和校验后原子切换，继续保留切换前失败与切换后清理重试语义。
- `/admin/works?tab=featured` 使用置顶、上移、下移、置底维护完整精选集合；服务端单事务校验集合和资源版本，冲突返回 `FEATURED_ORDER_CONFLICT` 并由前端重新加载，不做部分覆盖。
- 新精选自动追加、取消精选和删除作品自动压紧，历史重复或空洞序号在下一次写操作归一化为连续 `0..n-1`；用户界面只显示第 1、2、3 位。
- 公开首页共享上限改为 12，保留横向轨道和图片懒加载；超过 12 个已发布精选仍可保存，公开端只投影前 12。

## 首次失败与修复

- 首次相关 E2E 构建发现跨目录相对导出无法在 Nuxt 构建别名中解析；改为共享常量的 `~~` 别名后重放通过。
- 精选成员关系返回 409 时，原生 checkbox 会保留点击后的视觉状态；失败路径改为重新加载服务端列表，确保界面恢复真实成员关系。
- 品牌套件之后运行首页套件时，测试媒体夹具把 Hero 衍生图错误生成在水印 profile 下，清理旧水印会连带删除 Hero 变体并使首页聚合失败；夹具改为只给作品图生成水印变体，Hero 使用 `site-display` 配方。先按跨套件顺序重放 12 项通过，再完成相关 60 项整轮回归。
- 失败注入场景中的预览 500、媒体生成失败和清理失败日志属于测试明确构造的恢复路径，不是未处理的门禁失败。

## 验证证据

- `$env:APP_ENV='test'; pnpm lint`：通过。
- `$env:APP_ENV='test'; pnpm typecheck`：通过。
- unit：39 个文件、196 项通过；其中新增移动/置顶和 `[0,1,2,3] -> [3,0,1,2]` 等纯函数覆盖。
- integration：24 个文件、198 项通过；覆盖空库激活、原子切换/失败恢复、完整集合排序/冲突和公开前 12。
- `pnpm exec playwright test tests/e2e/admin-branding.spec.ts tests/e2e/admin-works.spec.ts tests/e2e/public-home.spec.ts`：60/60 通过；覆盖 390x844、768x1024、1440x900 单列布局、无横向溢出、键盘、焦点、减少动效、精选冲突恢复及第 7-12 件横向访问/懒加载。
- `pnpm exec playwright test tests/e2e/t09-ui.spec.ts --grep '基础字段修改与还原均影响 dirty'`：1/1 通过。
- `$env:APP_ENV='production'; pnpm build`：通过，包含生产内容守卫。
- `$env:APP_ENV='production'; pnpm run verify:production`：通过。

## 开放门禁

- T49-R1 必须对最终提交 SHA 重新做独立 Review；本轮实现者不能代签。
- 用户仍需验收真实管理数据下的单列排版、Logo 选择和精选编排手感。
- 本地工作树尚未提交或推送，新 SHA 的远端 Actions 状态未知；未经明确授权不提交、不推送、不发布。
