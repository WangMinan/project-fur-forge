# T16 独立 Review 记录

> **日期**：2026-08-13。
> **实现基线**：`19af5878e4bc5c2b01316f9cb89082b90cce0e46`。
> **最终结论**：`PASS WITH USER FOLLOW-UP`。
> **边界**：本记录关闭独立工程 Review，不代签 T17 的真实平台账号、物理手机扫码和用户体验验收。
> **当前覆盖说明**：本记录只对应上方历史实现基线。需求2等价补丁现已包含在 `main`；T17-F1～F4 及后续 `main` 改动仍须 T16-R1 新 SHA 独立复查，当前状态见 [`../STATE.md`](../STATE.md)。

## 1. 方法与范围

- 从新上下文核对 `SPEC → PLAN → TASKS → 代码/迁移/媒体投影 → 测试与运行页面`；
- 使用真实浏览器分别检查公开 `127.0.0.1` 与管理 `localhost`，覆盖 `390×844`、`768×1024`、`1440×900`；
- 对首页标题层级、重复搜索控件、渠道卡片、圆角、字体尺度、间距和横向溢出进行目视及 computed style 检查；
- 回归首页轮播真实 10 秒切换、联系二维码 FFmpeg 自动适配、搜索组合、动态发布状态与 Host/私有字段边界；
- 使用文档读者契约检查 `.design` 与需求2活文档能否准确回答当前导航、页面、数据和剩余门禁。

## 2. 首次 findings 与修复

首次结论为 `NOT PASS`，功能与视觉未发现阻断缺陷，但文档存在以下合并阻断：

| 优先级 | Finding | 修复与复查 |
| --- | --- | --- |
| P1 | `.design` 仍使用旧导航和页面树，缺少“最新动态”、三个目录搜索及后台“动态管理” | 已同步公开/管理 README、Design Brief、IA，并修正 `PUBLIC_NAV_ITEMS` 与 `AdminShell` 的过期说明注释；复查后与实际路由和组件一致 |
| P1 | SPEC 已实现的搜索、动态、三视口和状态投影仍未勾选 | 已按 T07～T15/T16 证据逐项回填；真实平台账号与物理手机扫码拆分并保留给 T17 |
| P2 | REVIEW、PLAN、STATE、ARTIFACTS 和 models 仍称 T02～T15 或 T16 未完成 | 已同步到 T16 完成状态，并保留 PR 合并和 T17 未完成边界 |

用户明确授权修复以上三组问题。修复仅涉及设计/需求/评审文档和两处说明注释，没有改变运行时行为、数据模型、迁移或测试断言。

## 3. 功能与安全结论

- 首页 Hero 在鼠标停留和控件聚焦时仍自动播放；真实等待约 10.2 秒后从第 1 张切换至第 2 张。显式暂停、页面隐藏和 reduced-motion 门禁保持；
- contact QR 接受 PNG、JPG/JPEG、WebP 及任意长宽比，任一边至少 64 px、最大 20 MB；内嵌 FFmpeg Lanczos 等比 contain 到 `640×640` 白色私有适配源，不裁切，原图保留；
- 公开二维码仍只投影 READY 的 320/640 无水印 PNG，不泄漏原图、私有 Key、预处理对象或签名 URL；
- `/works`、`/adoptions`、`/returns` 共享 `q` 契约并在分页前过滤；筛选、分页和返图 seed 的保留/清除符合 SPEC；
- 最新动态仅实现方案 B；草稿和下架记录在 SQL 层排除，首页只读取最近三条 published 动态；
- Host、Origin、CSRF、日志、analytics 和私有媒体边界未发现放宽。

## 4. 视觉结论

- 三视口下首页“精选作品 / 委托与领养 / 当前领养 / 最新动态”的标题字体、字号、字重、行高和标题基线一致；
- 作品、领养、返图三个页面的搜索输入、提交/清除控件使用同一组件、`var(--radius-sm)`、基础字体尺度和最小触控高度；
- 公开页同级 H1、筛选圆角矩形和字体尺度一致，未发现横向溢出或主要视觉元素漂移；
- 联系渠道在 390/768/1440 下为 2/3/5 列；只有两条渠道时保持既有阅读宽度并左对齐，不横向拉伸；
- 图片继续是公开端最高视觉层级，没有被动态文字、搜索控件或后台状态语言抢占。

## 5. 验证证据

| 验证 | 结果 |
| --- | --- |
| `pnpm lint` | PASS |
| `APP_ENV=test pnpm typecheck` | PASS |
| `APP_ENV=test pnpm test` | 35 files，179/179 PASS |
| `APP_ENV=test pnpm test:integration` | 24 files，189/189 PASS |
| `APP_ENV=production pnpm build` | PASS，含生产内容防泄漏守卫 |
| `public-home.spec.ts` + `admin-content-sections.spec.ts` 定向 E2E | 29/29 PASS |
| Edge 全量 E2E（实现记录） | 239/239 PASS |
| 实现基线远端 Actions run `31628640863` | `checks`、`image-build`、`e2e` 全部成功 |

T16 文档修复形成的新 PR HEAD 仍须在合并前查询其实际远端检查；不能把 `19af587` 的结果沿用到后续 SHA。

## 6. 剩余门禁

- T17 由用户填写或确认五个平台真实账号与实际二维码；
- 使用真实手机相机抽查二维码可扫并核对落地目标；
- 用户确认导航、搜索、最新动态与联系区的完整体验；
- T16-R1 与 T17 分别通过后再更新最终闭环状态；旧 PR #10 只保留为历史实现上下文，不再作为当前代码合入门禁。
