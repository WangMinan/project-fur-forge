# V05 · Homepage Shared Design Language / Tokens / Media Rules Handoff

日期：2026-08-23  
范围：公开端最小语义 token 与媒体边界；不修改上传、Schema、发布、水印或 Footer。

## Completed

- 复核现有 spacing、type、radius、focus、motion duration/easing 已足够，未创建第二套设计系统。
- 新增三个真实重复语义：`--public-editorial-ink`、`--public-background-type`、`--public-media-canvas`。
- Featured 黑色行动/静止背景字与 Adoption 首页/目录 canvas 改为消费语义 token，删除对应硬编码。
- 明确媒体边界：Hero/Featured/Commission 为摄影 `cover`；Adoption 为设定图 `contain`；Works 目录保留真实 3:4 / 16:9 混排。
- Directional Motion 的共用部分继续由现有 feedback/state/content/media duration 与 standard/playful easing 承担；场景位移不强行共用。

## Evidence

- `implementation/evidence/V05/homepage-1440x900.png`
- `implementation/evidence/V05/homepage-390x844.png`
- `implementation/evidence/V05/homepage-430x932.png`
- `implementation/evidence/V05/audit.json`

## Verification

- `node scripts/capture-v05-evidence.mjs` 通过。
- 三种视口语义 token、四级 motion token 与四幕 object-fit 规则一致，无水平溢出。
- `/works` 同时存在 portrait/landscape 且保留 0.75 / 1.7778 比例；`/adoptions` 使用 canvas + contain。
- `pnpm lint`、`pnpm typecheck`、`git diff --check` 通过。

## Locked Decisions

- Token 只表达共享语义，不抽象各 Scene 的媒体位置、轴线、overlap 或位移。
- Featured 3:4、Commission 摄影和 Adoption contain 不互相传播。
- 不新增依赖、上传规则、媒体变体或水印策略。

## Open Issues

- Catalog/Detail 的完整视觉节奏属于 V06 / V06-F1。
- 全站 prefers-* 与性能统一复核属于 V11。

## Regression Risks

- 将 `--public-media-canvas` 当作所有图片占位色会让摄影场景读成普通卡片。
- 把所有媒体改成同一 `object-fit` 会破坏竖版作品或设定图完整性。
- 为每个局部颜色继续新增 token 会制造无意义抽象。

## Next Task

`V06 · Works / Adoption Catalog 内容节奏`

## Do Not Start Yet

- V06-F1 Unified Detail
- V07–V12、T47、GATE-E
