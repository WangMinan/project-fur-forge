# 当前状态：需求4 · 站点视觉升级与内容合规

> **角色**：整个需求的状态机与收敛中枢（对应 spec-kit `/converge`）。
> **规则**：每轮工作开始时读、结束时回写；任务勾选以 `implementation/TASKS.md` 为准。
> **基线**：`main@913d257281e0b6a7ca60711cc62b78534904c6bd`。

## 当前阶段

阶段 3 · 地基、规格、模型、设计、计划和任务已经锁定；应用代码、数据库迁移、默认文案与生产数据尚未实施需求4。

## 最近验证

- 2026-08-19：按 `agent_docs/_template` 建立完整文档集，对照需求1～3活文档与 `main@913d257` 复核模块边界。
- 2026-08-19：复核首页聚合、Hero 横竖集合、站点展示配方、既有 `assets.focal_x/focal_y`、委托投递 Schema、隐私/条款默认文案与 `/licenses` 实现。
- 2026-08-19：评估 `emilkowalski/skills` 的 Apple Design Skill；采纳即时反馈、空间一致性、克制、可中断交互、响应式排版和 reduced-motion 原则，不把手势物理或新动效库设为默认实现。
- 2026-08-19：完成文档交叉 Review；品牌、渠道、首页四幕、移动端、焦点、隐私、人工清理、服务条款、QQ 确认、开源声明与 Git 规则口径一致。

## 当前约束

- 英文品牌固定为 `DITE DOG`，不得恢复 `DITE DOG FURSUIT` 或“暂用英文名”。
- 首页继续覆盖完整核心业务，但必须采用“品牌 Hero → 代表作品 → 自设委托 → 设定领养”的编辑式四幕编排，不把全部内容降格为同权卡片列表。
- PC Web 是视觉方案的第一展示基准；移动端必须提供等价重排，不依赖 hover，不使用 scroll-jacking、长时间 pinned scroll 或强制横向叙事。
- 公开动效服务注意力交接、空间关系和操作反馈；默认不引入专用 spring/手势库，不制造持续漂浮、过度玻璃、视差或大面积全屏运动。
- Hero 焦点复用既有资产 `focal_x/focal_y`；后台首版只提供可选九宫格预设，默认居中，不建设任意裁切编辑器。
- 站内表单负责结构化投递；后续沟通和逐单确认优先使用本网站公布的工作室官方 QQ 私聊；邮箱为备用渠道；QQ群不作为默认订单确认渠道。
- 申请人必须确认已满 18 周岁；不建设身份证、出生日期或监护人验证。
- 申请确认只覆盖隐私告知、成年声明和“提交不等于接单”；逐单价格、定金、排期、修改、交付与特殊约定继续在官方 QQ 中确认，网站不建设在线合同、订单或支付。
- 委托原始个人信息与私有设定图不得永久无条件保留；采用人工周期复核、受控工具执行删除，不建设定时任务、Worker 或通用生命周期引擎。
- 失效上传至少每月人工清理；待处理申请至少每半年复核；已拒绝/放弃申请处理满 180 天后进入删除候选；已接单申请在业务、保修、争议和法定必要期限结束后由人工确认删除。
- 删除工具默认 dry-run、精确对象、强确认、幂等；不得只删数据库或只删 OSS。
- `zhuohei-collage.ttf` 的来源为 Lemi Font，来源页面声明免费商用；它作为第三方授权资产留档，不虚构为开源许可证。
- FFmpeg 仅在工作室自有服务器容器内运行，不向访客或第三方分发二进制/镜像；若分发边界改变，必须重新评审许可证义务。
- 现有流水线和 main 规则保持不变；不新增 GitHub required check，不把需求4拆成新的强制 workflow。
- 需求1的 Host、媒体、OSS/ESA、安全、发布、恢复和部署基线，以及需求3仍适用的业务简化与匿名上传安全边界继续生效。

## 待确认问题（OQ 汇总）

无（所有阶段 OQ 已答）。生产启用前仍须由操作员填写真实经营主体名称，并由工作室最终审阅服务条款和隐私政策成文。

## 下一步交接

从 `implementation/TASKS.md` 的 A 阶段开始：先完成个人信息处理者配置、申请确认版本、隐私/条款文案和人工删除能力，再推进视觉与首页重构；任何视觉工作不得绕过隐私发布门禁。
