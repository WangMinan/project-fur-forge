# V12-A Handoff · Works Catalog & Work Detail

状态：V12-A implementation、Desktop/390/430 static evidence 与回归验证已完成。强制停止，V12-B 未开始。

## Completed

- `/works` 已改为等权作品目录：Desktop 四列、Tablet 三列、当前 Mobile Structural Safety 为两列，不再按角色、媒体方向或内容类型分配视觉权重。
- 作品卡统一使用 4:5 圆角媒体画布；竖版摄影保持填充，横版设定图使用 `contain` 完整展示。角色名称与物种进入图片底部渐变信息层，整卡保持可聚焦、可点击。
- `/works` 页头以背景 `WORKS`、中文标题/说明与右侧搜索建立独立 archive/editorial identity；搜索结果数只在搜索生效时显示，既有搜索、分页、路由与空态行为保持。
- 统一 Work / Adoption Detail 在 Desktop 保持左侧媒体 gallery + 右侧 sticky 信息账本，不改为上下长页；gallery 舞台与缩略图统一圆角，单图仍尊重原图比例，多图使用稳定媒体画布。
- 领养详情继续显示真实领养状态、价格、联系咨询入口与返回领养目录，并保留 V11 的媒体优先级和路由语义。
- 公共布局改为纵向 flex，只负责让短内容页 Footer 贴近视口底部；Footer 组件本身未修改。

## Locked Decisions

- `/works` Desktop 使用四列等权网格；不以卡片尺寸表达角色重要度，也不恢复按媒体比例自动分配列宽的布局。
- 统一卡片画布和圆角属于当前共享媒体语言；横版设定图完整性优先，不强行 `cover`。
- Work / Adoption Detail Desktop 继续使用左右构图，角色媒体与必要信息尽量在一屏内共同成立。
- V12-A 的 390/430 结果仅是 Mobile Structural Safety Evidence；Mobile Final Art Direction 仍属于 V12-G，不得提前代签。
- 未新增数据库字段、迁移、API、依赖或后台配置；作品搜索、分页、详情路由和领养业务投影不变。

## Open Issues

- 无 V12-A 阻塞项。Mobile 目录与详情的最终构图仍按既定顺序留给 V12-G。

## Regression Risks

- 竖版摄影为填充统一画布可能产生边缘裁切；横版设定图继续 `contain`，不得为追求铺满而回退为裁切。
- `WorkDetailGallery.vue` 同时服务普通作品与领养详情；后续改动必须同时验证单图、多图、横图、竖图和领养专属信息。
- 公共布局承担短页 Footer 贴底；后续页面不得用固定高度或绝对定位覆盖这一行为。
- V12-B 及后续任务不得改写 V12-A 的搜索、分页、路由、媒体回落或 V11 领养详情契约。

## Verification

- `pnpm typecheck`：通过。
- `pnpm lint`：通过。
- `pnpm test:core`：53 files / 314 tests passed。
- `node scripts/capture-v12-a-evidence.mjs`：通过。
- 自动审计：所有目标路由 200、图片全部解码、无横向溢出、Reduced Motion 下无运行中动画；Desktop `/works` 为四列，Desktop 详情为左右布局，短详情 Footer 贴底。
- Evidence：`implementation/evidence/V12-A/before/`、`implementation/evidence/V12-A/after/`。

## Next Task

- V12-B · About / Contact Static Redesign。新一轮开始前必须先读取 `TASKS.md` 与本 Handoff。

## Do Not Start Yet

- 本轮不开始 V12-B 页面实现，不实施 V12-C～V12-E、V12-E-F1、V12-F～V12-G、任何 Gate、V13～V16 或 T47。
- 不新增数据库、迁移、依赖、云端媒体操作、部署、提交、推送或 PR。
