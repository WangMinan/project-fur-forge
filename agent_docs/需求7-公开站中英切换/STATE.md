# 状态：需求7 · 公开站中英切换

> 状态事实以本文件为准；执行勾选以 [TASKS](implementation/TASKS.md) 为准。

## 当前阶段

阶段 5 · 基础 i18n 与后台多语言配置已完成本地实现和自动验证；用户已人工验收前序功能；本轮导航统一与 X 配置已完成实现、迁移及本地自动验证。

2026-09-12 用户同意本地只读评估方案，授权新建分支和需求目录，并补充：右上角语言图标展开选项，只支持中文与英文，默认 fallback 统一为中文。当前分支为 `codex/r7-public-i18n`，基于 `b163926676cea9d3a1d5c4e53c841f47c0c4c0a5`。

## 最近验证

- PR 收尾：语言 hover/触屏/键盘、英文委托标题 390–1920px 边界与下拉字号一致性通过真实 Chrome 验证；4 项相关浏览器用例及字号后 2 项复验通过。62 份 core 文件共 310 项具备通过证据（图片处理套件超时后独立 21 项复验通过）；本轮未重复 auth-api/health 两个未改动的运行时构建套件。lint/typecheck/build/生产 guard 通过，字号微调后定向 lint 与浏览器复验通过；48 处文档链接通过。

- 2026-09-12 ponytail 冗余审查：全仓引用/重复扫描后清理确认死代码与重复实现，代码净减 669 行（测试净减 509 行），保留原有用例。lint/typecheck/build、329 项 core（唯一过时迁移计数断言修正后定向复验）及 2 项 Chrome smoke 具备通过证据。详见 [冗余审查记录](implementation/notes/2026-09-12-PONYTAIL-REVIEW.md)；不代签独立完整实现 Review。

- 最新微调：语言菜单与关于下拉共用 10rem 宽度、选项左对齐，覆盖上一条居中要求。lint/typecheck、Chrome 公开 i18n 回归通过，包含桌面两菜单实际宽度一致断言和窄屏溢出检查；截图已更新。

- 主站语言菜单按最新要求去掉勾号、居中并仅以文字颜色区分当前项，保留 aria-pressed。空库初始化 X 默认地址断言及 20 项数据库用例、lint/typecheck、公开 i18n Chrome 用例通过；390/1440 语言菜单截图已更新。

- 本轮最终 lint/typecheck/build（含生产 guard）通过；38 项受影响 core、3 项 Chrome 用例含单独复验均通过。0054+0055 的 Ubuntu SQL 验证和本地 0055 迁移/FK/integrity 通过；44 处本地文档链接及 diff 空白检查通过。生产未执行。

- 用户已明确表示前序其他功能人工验收通过。本次仅追加三项：站点配置分组/语言及委托导航统一为大图页分段样式，X 地址在联系方式中配置。保留生产及独立 Review 状态，不据此推定生产已验收。

- 2026-09-12 追加后台配置：lint/typecheck/build、38 项受影响 core 与 2 项 Chrome smoke 通过；Ubuntu 3.37.2 SQLite 的 0053→0054 跨平台 SQL 验证通过。本地库已完成迁移并核验 integrity/FK；生产未执行。详见 [后台配置交接](implementation/notes/2026-09-12-ADMIN-COPY.md) 和 [生产迁移说明](implementation/PRODUCTION-MIGRATION.md)。

- 2026-09-12 收尾：最终 lint、typecheck、build/生产内容 guard 通过；328 项 core 分批复验全部通过，15 项相关浏览器用例含复验全部通过。11 份需求文档的 58 处本地链接与 diff 空白检查通过。

- 2026-09-12：15 项相关浏览器用例全部通过（含首页超时及新增测试选择器修正后的复验），覆盖上传忙碌期禁止切换、同页输入/文件保留、13 项 sitemap、分页与中英/管理边界。
- 2026-09-12：定向 core 24 项通过；完整 core 串行 318 项通过；认证套件因共享资源下构建触及 240s 门槛，单独生产模式复验 10 项通过，合计 328 项全部具备通过证据。最终 lint、typecheck 和 pnpm build（含生产内容 guard）均通过。
- 2026-09-12：Linux/x64 冻结 lockfile 安装后，第三方声明生成与检查通过；tosource 精确版本/许可证字节校准有失败拒绝测试。

- 2026-09-12：`git fetch origin` 后确认 main 与 origin/main 一致、原工作树干净；新分支创建成功。
- 2026-09-12：10 份 Markdown（含需求导航）的 52 处本地链接、模板占位和行尾空白检查通过；核对覆盖条款的章节编号；3 张参考截图原样留存且哈希一致。未安装依赖或执行应用测试、构建。
- 前序只读评估确认：现有 sitemap 取目录第一页、分页 canonical 丢失 page、公开 DTO 仍可能包含价格；服务端错误页内部请求未传递语言信息。它们是待实施项，不是已修复事实。

## 当前约束

- 用户已授权在本分支修复、commit、push 与创建 PR，并确认把现有 OSS/媒体清理和测试迁移 helper 重构一并纳入。未授权合并、镜像发布或生产部署。
- 只有 `zh-CN` / `en`；已保存选择优先，其次浏览器匹配，所有无匹配、缺失或无效语言偏好均回退中文。
- 同 URL 切换，不增加语言前缀、语言查询参数或域名；后台保持中文。
- 作品名称、物种、图片替代文本保持原文。用户已追加授权后台多语言文案、数据库与初始化内容调整；0054 前向迁移保留中文并初始化英文。
- 英文申请入口统一引导 X；中文表单和既有服务端准入保留；公开价格停止投影但后台金额保留。
- 继承需求6构图、详情显隐、媒体安全与公开资格，不更改需求6完成状态。

## 待确认问题（OQ 汇总）

无阻塞规格或计划的问题。英文 slogan 采用规格所列候选进入文案校对，最终措辞和视觉效果在人工验收中确认；这不构成语言基础接入的前置问题。

## 下一步交接

本轮后台配置扩展已完成本地验证及生产迁移文档交接。用户已确认前序其他功能人工验收通过。本轮新增导航与 X 配置按追加需求验证；独立 Review、未报告的真实媒体指标和生产现场核验分别记录，不由人工功能验收代签。本轮完成提交、推送和 PR 交接；不合并或发布部署。
