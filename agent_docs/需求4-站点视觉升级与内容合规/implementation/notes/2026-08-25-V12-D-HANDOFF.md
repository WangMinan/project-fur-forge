# V12-D Handoff · Legal / Privacy / Licenses Reading Design

状态：V12-D implementation、Desktop/390/430 长页与许可证展开态 Evidence 已完成。强制停止，V12-E 未开始。

## Completed

- `PublicPageIntro.vue` 增加独立 `document` 变体和事实型 metadata；默认变体保持不变，不影响申请页等既有内页。
- `PublicLegalDocument.vue` 为服务条款和隐私政策提供动态章节数、编号章节、章节导航、Desktop sticky 目录、46rem 正文轴和返回页首。
- `/licenses` 复用相同阅读网格，显示授权资产/依赖记录事实，保留 FFmpeg、第三方资产、npm 生产依赖和原生 `details` 许可证全文。
- 章节锚点让开 Header；移动目录和折叠摘要保持至少 44px；正文无横向溢出，图片完整解码。
- Evidence 脚本覆盖三路由的 Desktop、390、430，以及许可证键盘展开态；截图前恢复页首，避免 sticky Header 因自动滚动产生全页截图伪影。

## Locked Decisions

- V12-D 只建立长文阅读结构，不修改服务条款、隐私政策、FFmpeg 声明、第三方资产、生产依赖或许可证全文。
- `/terms` 等既有重定向、Footer、Header、公开路由、后台内容来源和业务行为保持不变。
- 原生 `details` 继续作为许可证长文开合控件；不引入自制折叠组件、依赖、卡片系统或 Motion。
- V12-D 的 390/430 结果是结构安全证据，最终 Mobile Art Direction 仍属于 V12-G；全站字体角色治理属于 V12-E-F1。

## Open Issues

- 无 V12-D 阻塞项。

## Regression Risks

- 服务条款和隐私政策章节数量来自后台正文解析；标题格式变化可能改变目录结构，但正文内容仍原样投影。
- Desktop 目录依赖 `--public-header-height` 保持锚点和 sticky offset 一致；后续 Header 调整不得另写第二套偏移。
- 许可证生产依赖数量由生成的 summary JSON 决定；更新依赖后 metadata 会同步变化，不应写死当前 `796`。

## Verification

- `pnpm check:fast`：通过；包含 lint、typecheck、53 个测试文件和 314 项测试。
- `node scripts/capture-v12-d-evidence.mjs --verify`：通过。
- 自动审计覆盖 `/service`、`/privacy`、`/licenses` 的 1440×900、390×844、430×932：路由 200、图片解码、无横向溢出、Footer 存在、正文宽度不超过 768px、Mobile 导航目标不小于 44px、章节锚点安全和 `details` 键盘开合均通过。
- Before/After Evidence：`implementation/evidence/V12-D/before/`、`implementation/evidence/V12-D/after/`。

## Next Task

- V12-E · Error / Empty / Media Failure Static States。新一轮开始前必须先读取 `TASKS.md` 与本 Handoff。

## Do Not Start Yet

- 本轮不开始 V12-E 页面实现，不实施 V12-E-F1、V12-F～V12-G、任何 Gate、V13～V16 或 T47。
- 不新增数据库、迁移、依赖、云端媒体操作、部署、推送或 PR。
