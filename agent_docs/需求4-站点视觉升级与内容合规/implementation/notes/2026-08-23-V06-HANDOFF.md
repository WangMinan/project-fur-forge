# V06 · Works / Adoption Catalog 内容节奏 Handoff

日期：2026-08-23  
范围：公开作品与设定领养目录的视觉节奏、空态、输入边界与验证证据；不修改搜索/排序、DTO、数据接口、详情链接、Footer 或业务字段。

## Completed

- `/works` 保留真实 3:4 竖版出厂照与 16:9 横版封面混排，桌面按比例等高铺满，移动端收敛为单列；`/adoptions` 保留设定图完整性与响应式双列/单列节奏。
- Works 的无作品、无匹配、非法搜索和越界页空态统一复用现有 `PublicEmptyState`，并保留明确的清除/回到第一页行动；Adoption 空态继续复用同一组件。
- 搜索、分页、联系入口、卡片详情链接和公开查询参数未改变；搜索输入、提交、清除、分页和联系行动均保持至少 44px 可操作尺寸。
- 修正 `capture-v06-evidence.mjs` 的懒加载采集：逐卡滚动并等待图片解码后回到页首再截图，避免把占位状态误记为页面失败；证据同时覆盖键盘提交、触控提交、非法搜索、越界页和 reduced motion。

## Evidence

- `implementation/evidence/V06/works-1440x900.png`
- `implementation/evidence/V06/works-390x844.png`
- `implementation/evidence/V06/works-430x932.png`
- `implementation/evidence/V06/adoptions-1440x900.png`
- `implementation/evidence/V06/adoptions-390x844.png`
- `implementation/evidence/V06/adoptions-430x932.png`
- `implementation/evidence/V06/works-invalid-search-390x844.png`
- `implementation/evidence/V06/works-out-of-range-390x844.png`
- `implementation/evidence/V06/adoptions-long-identity-390x844.png`
- `implementation/evidence/V06/audit.json`

## Verification

- `node scripts/capture-v06-evidence.mjs` 通过：10 个路由/视口组合、图片全部解码、横竖混排、移动单列、搜索、无匹配、非法搜索、越界页、超长名称/物种、键盘、触控 44px target、reduced motion 与无水平溢出均为 true。
- `pnpm lint` 通过。
- `pnpm typecheck` 通过。
- `pnpm test:core -- tests/unit/public-pagination.test.ts tests/unit/public-search.test.ts` 通过（2 files / 3 tests）。
- `git diff --check` 通过。

## Locked Decisions

- 目录继续使用真实媒体比例，不新增瀑布流、无限滚动、排序或新筛选字段。
- Adoption 设定图继续使用 `contain` 与稳定 canvas；Works 继续按真实方向混排。
- 空态使用共享 `PublicEmptyState`；Footer 内容、布局、样式、响应式与交互冻结。
- 不新增依赖，不改变 DTO、API、详情路径或媒体生成/水印策略。

## Open Issues

- 统一详情页的媒体主视觉、缩略图切换和空图库属于 `V06-F1`，尚未开始。
- 最终真实手机验收、全站输入/无障碍/性能复核属于 `V11` 与 `T47`。

## Regression Risks

- 将 Works 的比例变量改成固定列数会破坏横竖等高混排。
- 把 Adoption 的 `contain` 改为 `cover` 会裁切设定图。
- 证据采集若不滚动懒加载卡片，会再次产生占位图误报。

## Next Task

`V06-F1 · Unified Work / Adoption Detail Scene`

## Do Not Start Yet

- V07–V12、T47、GATE-E
- 新增业务字段、详情灯箱、媒体拓扑或数据库迁移
