# 2026-09-12 · 后台多语言文案交接

## 范围与结果

用户同意可扩展后台配置设计，并明确允许数据库/初始化内容调整、要求生产迁移脚本。本轮继续使用 codex/r7-public-i18n，保留前序 i18n 改动。

- 站点配置按“公开文案 / 营业与联系 / 条款与隐私”组织；文案使用单一表单按语言加载，当前只开放中文/English。
- 首页 slogan、委托正文、关于正文、营业标签从数据库公开投影读取；英文旧正文移入一次性迁移初值，移除运行时语言 JSON 的这些正文。导航、控件、法律文档外壳和固定渠道规则仍是 UI 翻译资源。
- 中文保留原字段及原 API；其他语言按 locale＋section 存储，分区独立 CAS。home 新增独立版本；作品原文与媒体契约不变。
- 空译文输入框保持空白，提供中文参考；公开页逐字段回退中文，两边都空的可选段落隐藏。营业 tone 共用，标签独立维护，后台明确提示切换 tone 后核对各语言标签。
- 复用分区 Shell/字段/草稿 hook，移除不再使用的两个中式正文专用 Card；修正共享 hook 在服务端刷新后无法跟随原本干净字段的问题。
- 分组切换保留草稿，切换语言需确认丢弃，保存中锁定语言选择；冲突保留草稿供比较与重试，刷新/离开页面有未保存提示。
- 集中语言清单与 application/contact 业务模式，不开放日语/韩语或在线语言管理。新增语言无需扩列或复制表单，但必须补齐 UI 词条和匹配/渠道/布局验证。

## 验证证据

- 最终 pnpm lint、pnpm typecheck、pnpm build（含生产内容 guard）通过。
- 受影响 core：database、public-site-contracts、contracts 共 37 项通过；新增 site-copy 集成用例通过，共 38 项。新增用例覆盖 0053→0054 保留旧字段及版本、备份、重入不覆盖、语言/分区 CAS、严格字段与安全文本、中文回退、未启用语言隔离、FK/integrity、旧 journal 拒绝新库。
- 新增测试首轮误用了 CLI 输出字段 backupCreated，已改为 migrateDatabase 的实际 backupFile 返回契约并复验通过；实现无为测试放宽校验。
- 两项真实 Chrome smoke 通过：后台语言编辑/持久化/缺 CSRF 拒绝/未登录拒绝/取消语言切换/分组草稿保留/409 对比与重试/空译文刷新及 SSR 回退，以及原公开语言 SSR/偏好/业务分流/表单状态/响应式用例。
- 后台截图覆盖 390、768、1440，验证无横向溢出及语言控件可聚焦；人工查看 390/1440 受控截图。见 artifacts/screenshots/admin-copy-*.png。独立审美评审和真实手机不由此代签。
- Ubuntu-2204 / SQLite 3.37.2：将 Windows 创建的 0053 合成库复制到 Linux 临时目录，执行实际 0054 SQL；中文字段保留、独立版本、英文初值、未来语言记录、integrity/FK 均通过。这是跨平台 SQL 验证，不冒充生产冻结容器演练。
- 本地 .data/dev.db 核验已至 0054，迁移入口复跑 applied=0，英文四个分区存在，integrity=ok、FK 无异常；存在升级前自动备份。本轮未访问或迁移生产。

## 生产交接

执行 [生产迁移说明](../PRODUCTION-MIGRATION.md)，脚本已纳入 migration journal，复用冻结镜像 ops migrate。特别注意：旧镜像会拒绝包含 0054 的迁移历史，不能直接用旧镜像连接新库；选择保留 0054 历史的兼容修复镜像，或经授权把旧备份恢复到新路径并配套旧镜像。

未提交、推送、创建 PR、发布镜像或生产部署。工作室校对、独立 Review、真实手机与生产环境验证仍待对应阶段执行。

## 人工验收后：统一导航与 X 配置

用户确认此前其他功能人工验收通过，并要求三项调整。本轮已完成：

- 抽取大图页的既有分段样式到 admin-base.css；大图页面、站点配置分组、语言按钮和委托状态导航共用。原生按钮/链接分别保留 aria-pressed/aria-current、焦点与窄屏换行。语言下拉替换为中英分段按钮，保留草稿确认和保存中禁用。
- “营业与联系 → 联系方式”新增 X 主页链接。复用 contact 分区保存及冲突版本，旧请求缺少新字段时保留现值；校验 HTTPS/x.com 账号主页、拒绝脚本/外域/查询参数，末尾斜杠归一化。
- 公开 site-content.contact 和 home-aggregate.hero 投影同一配置；首页入口、关于页、委托页、申请页及账号显示全部消费该值，删除运行时 X 地址/账号常量。
- 新增 0055 前向迁移，不重写已应用的 0054。仅加 x_contact_url，初始为既有景宸账号。生产迁移说明已更新：0053 升级执行两步，0054 升级执行一步；仍受严格迁移历史与恢复授权边界约束。
- 38 项相关 core 通过；新增集成断言覆盖 URL 校验、跨分区版本、旧调用保留已修改地址、公开投影。语言/草稿、X 配置与导航、公开 i18n 三项 Chrome 用例均有通过证据。
- 首次并行构建期间语言用例超时，trace 截图为未渲染的空白开发页；构建完成后独立复跑两项后台用例通过（20.2s、7.3s），没有放宽断言或超时。复验包含带末尾斜杠的 X 地址保存、刷新及所有公开页链接验证。
- 390/768/1440 后台文案及 390/1440 委托导航无横向溢出，受控截图已更新；查看桌面配置和窄屏委托截图确认分段样式一致。
- Ubuntu-2204 / SQLite 3.37.2 上对合成旧库执行 0054+0055 实际 SQL，通过中文保留、X 初值、integrity/FK 验证。本地开发库执行 0055 applied=1，自动备份 pre-migrate-2026-09-12T08-37-19-497Z.db，integrity=ok、FK=[]。

本轮未提交、推送或操作生产。最终构建与静态检查结果见 STATE。

## 主站语言选项微调与初始化确认

按用户最新要求去掉主站语言选项的勾号及选中加粗，选项文字居中，仅用文字颜色区分当前语言；保留 aria-pressed、键盘和焦点行为。X 地址已在 0055 初始化迁移中，无需重复 seed 或重写历史迁移；为空库初始化测试补充 X 默认值断言，20 项数据库用例通过，lint/typecheck 通过。

公开 i18n Chrome 回归通过（20.3s），语言菜单截图已更新，包含窄屏与桌面；本轮没有新增数据库迁移或生产操作。

后续按用户最新截图要求，将语言选项调整为左对齐、与关于下拉相同的左右留白；两个菜单共用 --public-dropdown-width（10rem），覆盖此前居中要求。保留仅颜色选中与 aria-pressed。

## PR 收尾：交互与英文委托布局

语言菜单仅在鼠标且 hover/fine 设备上响应 pointerenter/leave；移动到选项不会关闭，触屏点击保持原生切换，鼠标退出不抢焦点。Escape 和语言选择恢复按钮焦点，导航时关闭，语言菜单与展开的关于菜单互斥。

英文 Commissions 原字号按 viewport 放大，长单词溢出了桌面左侧四列。现只对英文 identity 建立 inline-size 容器，标题按列宽缩放并提供长文本换行边界；中文和媒体列不变。

本轮四项 Chrome 用例通过，覆盖鼠标 hover、移出、键盘、真实 touch context 点按、后台译文/X 配置、公开 i18n，标题在 390/768/1024/1280/1440/1920 下保持文字列内，桌面与媒体不相交。截图 commission-english-390/1440.png 为合成媒体布局证据。

用户另行确认纳入现有清理：移除无调用的 OSS/媒体/备案枚举导出，测试历史迁移 fixture 共用 migrationsThrough/migrationsAfter helper；未扩展媒体行为。当前源码调用方搜索无残留引用。独立 Review 未由此代签，PR 交接见 STATE。

用户复核发现语言项继承正文 1rem、关于子菜单使用 0.875rem；语言项现显式复用 --font-size-sm，浏览器断言比较两者计算字号。

最终验证：62 份 core 文件，309 项首轮通过，work-publication 的一个 FFmpeg 用例在并行运行时触及 30s；独立复验该套件 21 项全部通过，累计 310 项具备通过证据，未修改超时。本轮 auth-api/health 两个未改动的 Nuxt 构建套件未重复执行。lint/typecheck/build/生产 guard 通过；最后字号微调另通过定向 lint 和两项公开 Chrome 用例（15.8s、4.3s），其中直接比较关于子菜单与语言选项的计算字号。全部四项相关 Chrome 用例均有通过证据。

## main 发布门禁修复

release-image 34686045809 在 Run release verification 失败：19 项 smoke 中 18 项通过，旧的“管理员可通过登录表单进入后台”用例直接在配置默认页查找联系方式，没有进入新版“营业与联系”分组。应用界面符合已验收规格。

按用户授权直接在 main 修复该用例，仅增加按可访问名称点击“营业与联系”的步骤，保留原有可见性、安全提示退役和移动导航焦点断言。排查其他联系方式消费者，已使用分组切换或 #content-contact 深链，无需改动应用或放宽测试。

复验：完整 19 项 smoke 全部通过（2.8m），定向 ESLint 与 Nuxt typecheck 通过。以 [skip ci] 修复提交避免重复普通 push CI，重新触发的 release-image 仍执行完整 check:fast/test:release 与镜像验证门禁；不跳过发布检查。
