# V12-E-F2 Handoff · Desktop Acceptance Corrections

状态：V12-E-F2 implementation、1440/390/430 Evidence、回归测试与 Design Review 已完成。重新停在 `GATE-DESKTOP`，等待凌巽人工验收；未放行前不得开始 V12-F。

## Completed

- `ResponsivePicture.vue` 的失败层改为覆盖实际媒体框，Logo 与短提示“无法显示”在 Hero、Featured、Commission、Homepage Adoption、Works/Adoption 卡片、统一 Detail 和缩略图内居中且不溢出。
- Homepage Adoption 删除“可领养”状态展示，将物种放入原事实位置；`/adoptions` 卡片与统一 Detail 删除领养状态展示。
- 公开领养投影只返回 `available`；`adopted` 自动退出首页和 `/adoptions` 搜索/分页，但继续保留在 `/works` 作品档案中。
- `/works` masthead 右上复用 `/adoptions` 的低对比度旋转工作室 Logo 水印。
- `/commission` 删除重复 kicker，制作范围与估价联系在 Desktop 使用 3:7 网格和淡灰竖线，在 Mobile 堆叠后使用淡灰横线。
- `PublicPagination.vue` 收口为共享 editorial 分页：箭头、中文文字、细线和当前页下划线；Works 与 Adoptions 继续共用同一组件。
- 修复 Homepage Adoption 动画 ref 指向 `NuxtLink` 引发的运行错误；ref 现在指向链接内部真实 DOM 容器，既有动画参数和时序未改。

## Locked Decisions

- 公开领养只投影 `available`；后台状态模型、数据库 Schema 与 `/works` 归档资格不变。
- 图片失败继续由 `ResponsivePicture` 统一处理，不为各页面增加独立回落组件、重试逻辑或占位资源。
- Works 与 Adoptions 必须继续共用 `PublicPagination`，保留 SSR、无 JavaScript、键盘、44px、current 和 disabled 语义。
- Hero 品牌文字、Footer、Motion、管理端字段、媒体 DTO、依赖与部署均未改变。

## Open Issues

- 无 V12-E-F2 实现阻塞项。
- `GATE-DESKTOP` 的凌巽人工静态审查尚未签字，这是下一步唯一开放事项。

## Regression Risks

- 后续修改共享 `ResponsivePicture` 定位上下文时，必须同时回归大媒体、目录卡片、详情舞台和小缩略图。
- 后续修改领养查询时，必须保持 `available` 公开投影与 `/works` 归档投影互不混淆。
- 后续 V14 若调整共享分页视觉，必须保留本轮验证过的链接、键盘、44px 与 current/disabled 语义。

## Verification

- `pnpm check:fast`：通过；test groups、lint、typecheck、53 个测试文件与 314 项测试全部通过。
- `node scripts/capture-v12-e-f2-evidence.mjs --verify`：通过。
- 自动门槛全部为 true：`availableOnly`、`noStatusCopy`、`noHorizontalOverflow`、`worksWatermark`、`commission`、`pagination`、`mediaCentered`、`noRuntimeErrors`。
- `git diff --check`：通过。
- Evidence：`implementation/evidence/V12-E-F2/after/`；覆盖 1440×900、390×844、430×932 的失败态、Homepage Adoption、Adoptions、Works、Commission、统一 Detail 与共享分页。

## Next Task

- `GATE-DESKTOP` · 凌巽人工全站 Desktop 静态 Art Direction 验收。

## Do Not Start Yet

- 未获凌巽本人明确放行前，不开始 V12-F、V12-G、`GATE-MOBILE`、V13～V16、T47 或 GATE-E。
- 不修改 Hero 品牌字、Footer、业务字段、数据库、媒体拓扑、Motion、依赖或部署。
