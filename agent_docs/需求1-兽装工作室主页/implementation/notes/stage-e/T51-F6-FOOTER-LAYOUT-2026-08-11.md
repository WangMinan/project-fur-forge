# T51-F6 公共页脚备案布局修复

> 日期：2026-08-11
> 基线：`48df504`
> 结果：实现完成，远端验证待流水线返回

## 范围

- 修复宽屏页脚左侧过松、右侧过挤的问题；
- 把公开导航与备案信息组成中间区，导航在上、备案在下；
- 把开源软件声明并入右侧法务行，使右侧保持版权与法务/设计署名两行；
- 保留现有 ICP/公安备案运行时 Schema、空值隐藏和登录页 `brandOnly` 行为。

不修改备案号码、运行时配置、法务正文、导航顺序、数据库或媒体链。

## 依据与实现

工信部《非经营性互联网信息服务备案管理办法》第十三条要求在主页底部中央标明备案编号并链接备案管理系统。公安联网备案尚未完成，因此不输出虚假占位；现有公安备案配置槽保留，取得正式编号后可在同一中间备案行显示。

`PublicFooter` 在宽屏使用 `max-content / 1fr / max-content` 三列和相同列间距；中间内容居中，右侧使用两个独立段落。小于宽屏断点时恢复自然纵向布局和换行，避免横向溢出。

## 验证边界

- 已执行 `git diff --check`，通过；
- 按用户明确要求，本机未运行 lint、typecheck、unit、integration、build 或 E2E，也未启动本地页面；
- push 后的新 SHA 必须以远端 Actions 实际结果为准，既有 SHA 的绿色结果不能代签本次修改；
- 本记录不是独立 Review，也不代签用户最终视觉验收。
