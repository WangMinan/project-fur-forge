# 任务清单：<需求标题>

> **角色**：PLAN 的可勾选分解（对应 spec-kit `/tasks`）；每个 `[ ]` 都是 agent 可独立完成的单元。
> **写入时机**：阶段 3，PLAN 定稿后。
> **规则**：完成即勾选；新增任务追加到对应分组；不删除历史项，放弃的改为说明原因。

## 当前目标

<!-- 一句话总目标，带上关键幂等/禁用约束提醒执行者。 -->

<一句话>。

## <分组一：如 数据模型>

- [ ] <任务>。
- [ ] <任务>。

## <分组二：如 接收器实现>

- [ ] <任务>。

## 验证

<!-- 静态验证在前，运行验证在后。 -->

- [ ] <静态验证：编译 / lint / 契约检查>。
- [ ] <运行验证：联调 / 幂等 / 边界用例>。

## 闭环结论

<!-- 全部勾选后填写；未在本轮覆盖的后续工作在此列出。 -->

- <结论>。
