# T01 实施记录：可运行的双访问面最小切片

## 任务与边界

- 日期：2026-07-28
- 授权原话：“好的 现在允许你进入阶段4 从T01开始实施”
- 本轮只实现 T01：Nuxt 工程、公开 SSR 首页占位、后台 CSR 登录占位、健康检查、根目录分层和测试入口。
- 未实现 T02 之后的运行配置、Host 隔离、数据库、认证、OSS 或生产视觉组件。

## 组件与目录边界

| 路径 | 单一职责 |
| --- | --- |
| `app/app.vue` | 只组合当前 layout 与 route page |
| `app/layouts/default.vue` | 公开访问面的最小布局壳 |
| `app/layouts/admin.vue` | 管理访问面的最小布局壳 |
| `app/pages/index.vue` | 验证公开首页可由服务器输出非空 HTML |
| `app/pages/admin/login.vue` | 验证 `/admin/**` 在浏览器端渲染 |
| `server/api/health.get.ts` | 提供无外部依赖的进程健康响应 |
| `shared/constants/project.ts` | 提供公开端和测试共用的非敏感项目常量 |

生产视觉仍以后续 T04–T08 和 `.design/` 为准；当前占位样式不构成视觉验收结果。

## 工具链

- Node.js：`v24.18.0`
- Corepack：`0.35.0`
- pnpm：`10.33.0`
- Nuxt：`4.5.1`
- Vue：`3.5.40`
- Nitro：构建输出 `2.13.4`
- TypeScript：`6.0.3`

首次依赖解析发现：

1. pnpm 阻止了 `unrs-resolver` 的未审批 postinstall；已在 `pnpm-workspace.yaml` 明确许可 `unrs-resolver`、`esbuild` 和 `@parcel/watcher`，并保持 `strictDepBuilds: true`。
2. 当时 npm registry 最新 TypeScript 7.0.2 超出 `@typescript-eslint` 的 `<6.1.0` peer 范围；改为当前受支持的 6.0.3，随后安装通过。

## 验证结果

| 命令 | 结果 |
| --- | --- |
| `pnpm install --frozen-lockfile` | 通过；锁文件无需更新 |
| `pnpm lint` | 通过；退出码 0 |
| `pnpm typecheck` | 通过；退出码 0 |
| `pnpm test` | 1 个文件、2 个单元测试通过 |
| `pnpm test:integration` | 1 个 Nuxt/Nitro 健康检查测试通过 |
| `pnpm test:e2e` | 3 个 Chrome E2E 通过 |
| `pnpm build` | Nuxt node-server 生产构建通过 |
| `pnpm verify:production` | `.output/server/index.mjs` 的健康检查、公开 SSR、后台 CSR 全部通过 |

生产产物检查确认：

- `/` 返回 2xx，原始 HTML 含公开首页标记和工作室临时名称。
- `/admin/login` 返回 2xx，原始 HTML 不含管理页面正文，浏览器执行后页面可见。
- `/admin/login` 返回 `X-Robots-Tag: noindex, nofollow, noarchive`。
- `/api/health` 返回 `{"status":"ok","service":"project-fur-paws"}`。

2026-07-29 提交前同步已确认品牌事实：公开页与管理端标题改用正式中文名“有点小狗工作室”，公开页英文眉题暂用 `dite dog`；内部包名、服务名和工程代号保持 `project-fur-paws`。

## 遗留与非阻断警告

- Nuxt/Nitro 4.5.1 在 Node 24 的集成测试与构建中输出 `DEP0155`，来源为上游 `@vue/shared` exports 的尾斜杠映射；未影响类型检查、测试或构建。
- 初次解析报告 WASI fallback 的 `@emnapi/*` peer 警告；Windows 本机实际使用原生 binding，全部门禁通过。后续依赖升级时继续观察，不在 T01 通过手工加入未直接使用的 WASI 依赖掩盖警告。
