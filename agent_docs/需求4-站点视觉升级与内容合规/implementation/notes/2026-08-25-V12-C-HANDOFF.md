# V12-C Handoff · Commission Apply

状态：V12-C implementation、Desktop/Tablet/390/430、输入法与完整表单状态 Evidence 已完成。强制停止，V12-D 未开始。

## Completed

- 按用户复核保留原竖向流程，不采用 Desktop 申请信息/设定图左右分栏；申请信息、设定图、提交确认和反馈继续顺序阅读。
- 页名、隐私说明和表单统一为 56rem 主轴；字段在 768px 以上内部双列，390/430 单列，身高与体重保持成组并显示独立单位。
- 原页头右侧孤立的“设定图只用于工作室内部评估”已并入表单首段隐私提示，信息不再重复或悬空。
- Desktop/Tablet 页名左侧复用现有工作室 Logo，以 5% 灰度水印逆时针旋转 15°并放大覆盖标题与内部评估说明；Mobile 隐藏该装饰，避免干扰紧凑首屏。
- 公开上传区使用紧凑圆角媒体面；预览保持完整 `contain`，更换/移除使用轻量文字行动，错误只在存在时渲染。
- 确认区、校验摘要、上传/核验/提交阶段、失败保留、成功回执和不可用状态保持去卡片化静态层级。

## Locked Decisions

- Apply 继续使用竖向阅读顺序；后续不得把 V12-C 恢复为 Desktop 左右两块表单构图。
- 全部字段、含义、客户端/服务端校验、私有上传、提交、两项确认、QQ/Email 联系路径、失败后保留输入与图片、后台内容投影和营业状态不变。
- 不新增数据库字段、Schema、迁移、API、依赖或公开媒体；Footer 未修改。
- V12-C 的 390/430 结果是当前结构安全证据，最终 Mobile Art Direction 仍属于 V12-G。

## Open Issues

- 无 V12-C 阻塞项。

## Regression Risks

- `ImageDropzoneCard.vue` 仍同时支持 admin theme；公开端圆角/行动样式必须继续受 `data-theme='public'` 限定。
- 表单错误会增加页面高度；不得用固定高度恢复空错误占位或让失败反馈覆盖字段。
- 软键盘安全证据使用 390×500 缩短视口模拟；真实手机输入仍由 V15 最终检查。

## Verification

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm test:core`：53 files / 314 tests passed。
- `node scripts/capture-v12-c-evidence.mjs --verify`：通过。
- `git diff --check`：通过。
- 自动审计覆盖 1440×900、768×1024、390×844、430×932：路由 200、图片解码、无横向溢出、Desktop 字段双列、Mobile 字段单列、错误摘要、图片预览、提交失败后保留图片、重试成功回执、不可用状态和 390×500 输入法安全均通过。
- Before/After Evidence：`implementation/evidence/V12-C/before/`、`implementation/evidence/V12-C/after/`。

## Next Task

- V12-D · Legal / Privacy / Licenses Reading Design。新一轮开始前必须先读取 `TASKS.md` 与本 Handoff。

## Do Not Start Yet

- 本轮不开始 V12-D 页面实现，不实施 V12-E、V12-E-F1、V12-F～V12-G、任何 Gate、V13～V16 或 T47。
- 不新增数据库、迁移、依赖、云端媒体操作、部署、提交、推送或 PR。
