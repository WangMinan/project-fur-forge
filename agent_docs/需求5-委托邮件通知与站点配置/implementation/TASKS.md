# 任务清单：委托邮件通知与站点配置

> `[x]` 表示有实际实现或验证证据，生产/真实手机/默认两个内部收件箱不代签。规格见 [SPEC](../requirements/SPEC.md)，顺序见 [PLAN](../planning/PLAN.md)。

## 当前目标

为成功的新自设委托发送包含完整业务文字与私有设定图的内部邮件，后台维护任意数量的有效收件邮箱，用户侧所有文字与界面保持不变。

## 立项与文档

- [x] D01：读取模板与当前基线，核对真实投递/配置入口，初始化九份需求5文档。
- [x] D02：确认发件账号、邮件内容与附件、用户侧及隐私文字保持不变，区分用户决定与默认工程方案。
- [x] D03：建立计划、模型与任务初稿，不提前执行配置写入或发信。
- [x] GATE-SPEC：用户明确同意规格并授权当前分支开始编码。

## 配置与数据

- [x] T01：接入 SMTP 运行时配置、错误隔离、环境示例和 Compose 注入；安全写入本地 `.env`，验证不跟踪、不输出凭据。
- [x] T02：新增收件列表与通知记录前向迁移、唯一约束及 repository；验证默认初始化、空列表保留与既有数据兼容。

## 管理界面与发送

- [x] T03：在 contact 卡实现收件列表、校验、保存/冲突；后台页名统一“站点配置”，证明公开 DTO 和公开联系方式不变。
- [x] T04：在创建委托共同事务内持久记录通知，事务外发送完整正文与私有原图，保持公开响应和业务状态不变。
- [x] T05：实现有限重试、领取、重启恢复、停用/移除接收人和删除协调；现有委托后台显示状态与失败重试。

## 验证

- [x] T06：通过 lint、typecheck、受影响 core 和 build；用虚构资料验证重复请求、事务回滚、部分失败、配置异常、恢复、数据删除和附件完整性。
- [x] T07：真实浏览器覆盖 390×844、430×932、768×1024、1023×900、1024×900、1440×900；检查键盘、焦点、偏好、console/network、图片 decode、布局稳定及水平溢出；回归用户侧业务文字与投递流程；真实手机及用户后台观感不代签。
- [x] T08：经明确测试发信授权，以虚构资料验证 QQ SMTP、用户限定的自发自收邮箱、原图附件与接近最大允许图像的编码后大小（默认两个内部邮箱未发测试）；分别记录接受与收到。
- [x] T09：完成作者实现复核、证据索引、STATE 与 Handoff；用户后台体验及独立人工 Review 不由作者代签。

## 闭环结论

- SMTP 与后台配置已实现，本地配置、迁移及两封授权自发自收联调完成；未提交、推送或生产发布。
- 发布与部署不是本轮文档任务的自动后续；若后续安排，按既有手册取得目标环境证据。

## 本轮 PR/镜像发布授权

用户已明确要求将当前分支通过 PR 合入 main，并运行 release-image。邮件文案与旧测试无冲突，本轮不新增测试或断言；远程发布结果单独核对 GitHub 证据，不代签生产部署。

## 2026-09-09 追加修复

- [x] T10：三个 SMTP 超时改为 600 秒，总计两次尝试；停止时取消连接，未确认的中断任务由管理员核实后重试。
- [x] T11：历史实现管理图片签发十分钟 OSS 公网 GET，按需自动恢复；浏览器直读方案已由 T13 的追加授权取代，会话被动检查和图片请求不延长八小时闲置有效期的保护保留。
- [x] T12：完成 core、构建、smoke 和图片专项浏览器验证，补齐媒体与部署契约；详见 [修复交接](notes/2026-09-09-MAIL-OSS-DIRECT.md)。
- 本次只授权通过 PR 合入 main，不继承上节历史镜像发布指令；不执行生产部署或真实邮件测试。

## 2026-09-09 管理图片回滚

- [x] T13：按用户追加授权直接在 main 恢复同源图片和原图新窗口预览；Hero 编辑、作品设定图、领养封面、委托详情使用 1280，出厂照 640，列表/卡片 320；完成针对性回归及文档同步，详见 [图片回滚交接](notes/2026-09-09-ADMIN-IMAGE-PROXY.md)。
- 用户验证完成后追加授权 commit、push main 并启动一次 `release-image.yml`；工作流启动不等于镜像发布成功。生产部署、云配置或真实邮件测试不在本次范围。
