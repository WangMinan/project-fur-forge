# 需求5验证证据

## 自动与浏览器证据

- 可运行 core：`tests/integration/commission-email.test.ts`；复用当前 SQLite 迁移与假媒体存储，覆盖配置、列表隐私、事务、附件、失败/重试、心跳/重启和删除。
- 可运行浏览器：`tests/e2e/commission-email.spec.ts`；使用项目 Playwright 隔离库与运行时假 OSS，不访问真实素材。
- `contact-{390,430,768,1023,1024,1440}.png`：后台列表及保存状态。
- `notification-{390,430,768,1023,1024,1440}.png`：委托详情的通知与失败反馈。
- 同名前缀的 `*-metrics.json`：实验室 CLS/LCP、可见图片 decode、水平溢出和浏览器错误计数；由重载/尺寸切换产生，不作为生产性能或真实设备结论。

## SMTP 实证

[smtp-self-test.json](smtp-self-test.json) 仅记录图像字节数、摘要、SMTP 状态与时间，不包含凭据、正文或收件列表。

| 测试 | 原图字节数 | SMTP | 人工收件 |
| --- | --- | --- | --- |
| 小图 | 922,283 | 接受 1 个获授权收件地址 | 用户截图确认正文与附件收到 |
| 大图 | 19,996,756 | 接受 1 个获授权收件地址 | 用户随后确认收到 |

两封均为用户限定的自发自收测试，未向客服或景宸发信。测试数据库为独立临时库，测试素材为程序生成图，不是实际申请。

## 平台与部署边界

- Linux/x64：冻结锁文件 production 安装、第三方声明生成和校验通过；只新增 Nodemailer 10.0.0（MIT-0）。
- Windows 本地：0052 迁移 1 项且自动备份，SMTP 配置 ready，readiness HTTP 200。
- 生产 build 与内容泄露防护通过；未运行镜像发布、生产迁移或云配置变更。

详细首轮失败与复跑结论见 [Handoff](../notes/2026-09-06-SMTP-HANDOFF.md)。

最终浏览器运行两项通过；12 份度量无非预期错误/水平溢出，可见图片 decode 全部成功，最大观测 CLS 0.00515、LCP 3976 ms。预期 409 响应有独立断言，不吞掉其它错误。
