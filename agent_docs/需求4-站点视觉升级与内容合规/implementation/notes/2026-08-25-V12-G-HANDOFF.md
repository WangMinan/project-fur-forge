# V12-G Handoff · Remaining Public Mobile Art Direction

状态：V12-G implementation、390×844 / 430×932 全公开状态 Evidence、1440×900 Desktop 冻结回归、自动审计与 Design Review 已完成。当前停在 `GATE-MOBILE`；未获凌巽本人明确放行前不得开始 V13。

## Completed

- 逐页审查 Works Catalog、统一 Work/Adoption Detail、`/adoptions`、`/commission`、About/Contact、Apply、Service、Privacy、Licenses、404/500、空态、搜索无结果与媒体失败。
- 保留各页面既有独立 Mobile 构图；没有把 Desktop grid 机械缩小，也没有套用新的统一页面模板。
- `WorkDetailGallery.vue` 的 Mobile 多图舞台由 `58svh` 收敛为 `clamp(17rem, 92vw, 24rem)`；五张缩略图在 390/430 保持单行且无横向溢出。
- 单图 Adoption Detail 继续按原比例完整展示；搜索、分页、表单校验、二维码、法务目录、许可证原生展开、路由与后台内容投影均保持。
- 390/430 全量公开状态和 1440 Desktop 冻结回归 Evidence 已独立写入 `implementation/evidence/V12-G/after/`。

## Locked Decisions

- Desktop Art Direction、Footer 与 Hero 品牌文字锁继续冻结。
- Mobile 继续使用原生滚动；不复用 Desktop 逐幕 wheel/翻页逻辑。
- V12-G 不新增依赖、Schema、迁移、路由、业务模型或公开内容字段。
- 多图 Detail 只收紧 Mobile 舞台高度；单图设定图仍以完整展示为第一优先级。

## Open Issues

- 无 V12-G 实现阻塞项。
- `GATE-MOBILE` 仍需凌巽本人查看最终 390/430 静态构图并明确放行；自动测试和 Design Review 不代签。

## Regression Risks

- 后续 Motion 不得用 transform 改写已冻结的 Desktop/Mobile 静态终态。
- Detail gallery 的舞台高度、缩略图宽度或 gap 变化时，必须同时回归 390/430 的五图单行和单图完整展示。
- V13 的跨页 shared-media transition 必须严格限制在 TASKS 指定四条白名单，且 Reduced Motion / 不支持 View Transition 时可靠回落。

## Verification

- `implementation/evidence/V12-G/after/audit.json`：`expectedStatuses`、`noHorizontalOverflow`、`regularImagesDecoded`、`allFootersPresent`、`minimumTargets`、`formValidation`、`legalDetails`、`mediaFailureRecovery` 全部为 `true`。
- `implementation/evidence/V12-G/after/desktop-audit.json`：同八项 1440 Desktop 冻结回归全部为 `true`。
- 390px 多图 Detail 主舞台约 345px，五张缩略图保持单行，横向溢出为 0。
- `pnpm check:fast`：通过；53 个测试文件、314 项测试，lint 与 typecheck 全部通过。

## Next Task

- `GATE-MOBILE · Full Mobile Static Art Direction Approval`。

## Do Not Start Yet

- 未获凌巽本人明确放行前，不开始 V13、V14、V15、V16、T47 或 GATE-E。
