# T34-C1 用户验收与阶段迁移（2026-08-07）

## 结论

用户已在浏览器中完成人工核对，确认阶段 C 的公开端、管理端和主要业务行为符合当前验收预期。基于这次用户验收：

- T26-F1、T27-F1、T30、T34、T34-F6、T34-F7、T34-F8 均按阶段 C 的交付边界记为完成；
- `GATE-C1` 通过；
- 项目允许进入阶段 D 的范围确认与实施；
- 阶段 C 的完成不再以 GitHub Actions 当前一次运行全绿为前置条件。

本记录只登记用户已经明确给出的人工验收结论，不代替正式环境发布、域名、TLS、线上 Compose、升级、回滚和恢复演练。

## GitHub Actions 非阻断遗留

记录时 `main` 基线为 `3984b4f181d5a3071a119affae34c1088a53b6f9`，`quality` workflow run `31139795670` 的结果为：

- `image-build`：成功；
- `checks`：在 `Production build` 步骤失败；
- `e2e`：因依赖 `checks` 而跳过。

该结果说明当前流水线仍未全绿。现有证据不足以在本记录中断言 Production build 的具体根因，因此不沿用此前“runner 未接单”的旧判断。

用户决定把这项流水线故障从阶段 C 的阻断项移到阶段 E 的上线前综合审查任务中处理。后续由 T49 负责修复最新 `main` 的 Production build、恢复 E2E，并取得 `checks`、`image-build`、`e2e` 同一 SHA 全绿。

## 阶段 D 入口

阶段 D 尚未开始编码，当前先确认范围。用户明确认为返图墙是阶段 D 的核心价值，因此推荐优先顺序为：

1. T35：返图模型与可选授权记录；
2. T36：返图上传、轻量水印、管理与公开墙；
3. 对 T37–T41 的实际价值逐项确认后再决定是否实施。

T38、T39、T40 当前保留在任务清单中等待讨论，不因本次阶段迁移自动授权实施。
