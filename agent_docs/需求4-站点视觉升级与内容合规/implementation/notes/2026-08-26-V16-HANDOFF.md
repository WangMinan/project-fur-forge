# V16 Handoff · Consistency & Evidence Review

状态：V16 implementation review、fresh responsive evidence、最终 Evidence Index、Design Review、文档一致性校准和交接前确认的 Homepage 离场闪帧修复均已完成。凌巽于 2026-08-26 明确要求修复后进入下一任务，V16 已确认并已开放 T47；GATE-E 未开始。

## Completed

- 对照 V09 Shared Visual Language、V00-F2 public surface matrix、V09～V15 Evidence 与全部 Handoff，审查 Homepage、Catalog、Detail、Commission、Apply、About/Contact、Document、Error/Empty/Media Failure 的 scene identity 与共享语言。
- 生成 375×812、768×1024、1280×800 三档 fresh evidence：15 个公开状态 × 3 = 45，媒体失败 3，500 3，共 51 张单页截图；另生成 3 张 contact sheet。
- V16 自动审计的 expected status、selector、horizontal overflow、semantics、image decode、runtime error、Footer continuity、Hero brand lock、media failure、server error 共 10 项 checks 全部为 `true`。
- 人工复核 54 张 V16 图像时未确认生产 UI 缺陷；随后用户在实际连续操作中补充发现 Homepage 非 Hero scene → `/works` 的单帧闪动。逐帧 DOM/视频证实：route 已先更新为 `/works`，旧 Homepage 尚未离场时 persistent Header 从 `fixed` 变为 `sticky`，重新进入文档流并把旧 scene 下推 77px，露出 Hero 尾部，不是跳进第三个页面。
- `PublicHeader.vue` 现用 displayed visual path 保留离场页面的定位状态，并在 Nuxt 页面交换 promise 完成后的下一帧提交新 path；旧 Featured 顶部全程保持 72px，最大离场位移从 77px 降为 0。Playwright smoke 新增真实布局位移断言；没有恢复跨页 media morph 或命名 View Transition。
- 修复文档契约漂移：`CLAUDE.md`、SPEC、PLAN、Design README 与 Shared Visual Language 现在统一采用 V11 首页最多三项 `available`、`/adoptions` available-only、V09+ Featured Type × Media、4s 页面内 carousel 和正式路由无跨页 media morph 的当前事实。
- 建立 `implementation/evidence/V16/INDEX.md`，串联 V00-F2 与 V09～V16 全部公共表面、Evidence、Handoff、状态覆盖和 OSS/license 来源。
- 复核第三方资产与 notices：Noto Serif SC 为 OFL-1.1；ZhuoHei Collage 与 critical subset 保留 Lemi 免费商用来源口径；production snapshot 记录 796 个包及 `ffmpeg-static@5.3.0` GPL-3.0-or-later。

## Locked Decisions

- V11 supersede T21 Homepage single-adoption projection：Homepage Adoption 最多展示三项当前 `available`，不足三项按真实数量退化；不得修回单项。
- `/adoptions` 只公开 `available`；`adopted` 保留在 `/works` 归档。搜索、分页和既有排序继续保持。
- V04 Featured 双图描述仅为历史 Evidence；Active Visual Contract 为 V09+ Type × Media、当前 switching 和 GATE-E。
- Hero、Featured 与 Homepage Adoption 保留 4s 页面内 carousel；正式公开路由只使用短 opacity 入场，不启用命名 View Transition 或跨页 media morph。
- Footer 内容、布局、样式、响应式和交互冻结；Hero `有点小狗工作室` 品牌身份、冻结终态与一次性首次入场保持锁定。
- V16 不开启新视觉方向，不增加依赖、Schema、迁移、业务字段或装饰元素。

## Open Issues

- 无 V16 implementation 或 evidence 阻塞项；V16 已获确认。
- 真实 iOS/Android 设备、连续交互/性能审查与最终用户观感仍未由 V16 代签；它们分别属于 T47 与 GATE-E。

## Regression Risks

- 若后续文档再次引用 T21 单项领养、V04 双图、available + adopted `/adoptions` 或 V13 shared-media 历史 Evidence 作为当前规则，会重新产生产品与实现漂移。
- 若恢复命名 View Transition，之前已删除的跨页媒体位移、首击闪动和返回路径问题可能复发。
- 若修改共享 typography、摄影圆角、wayfinding 或页面 intro，必须同时确认 scene identity，避免把 Catalog、Detail、Document、Form 与 State surface 模板化。
- 若触碰 Footer、Hero Brand Lock、1023/1024 边界或 safe-area 策略，V15/V16 证据结论需重新验证。
- persistent Header 的 overlay/sticky 状态必须继续跟随当前显示页面；若重新直接绑定 incoming `route.path`，Homepage 非 Hero scene 离场的 77px 闪帧会复发。

## Evidence Index

- `implementation/evidence/V16/INDEX.md`：V00-F2、V09～V16、公开状态和 license/source 的最终索引。
- `implementation/evidence/V16/audit.json`：三档 public-state matrix 与 10 项 hard checks。
- `.design/screenshots/v16-final-consistency/review-*.png`：51 张 fresh screenshot。
- `.design/screenshots/v16-final-consistency/contact-sheet-*.png`：Mobile、Tablet、Desktop 三张人工复核总览。
- `.design/DESIGN_REVIEW.md`：V16 prioritized review。
- `implementation/evidence/V16/transition-regression/`：Homepage → Works 的 before/after 单帧与逐帧 audit。

## Verification

- `V16_ERROR_BASE_URL=http://127.0.0.1:3007/ pnpm exec node scripts/capture-v16-evidence.mjs --verify`：0 checks failed；3007 仅为本次临时 test fixture，验证后已停止，常驻主站仍为 3000。
- `pnpm notices:check`：通过；使用既有 `D:\.pnpm-store\v11` 只读生成态完成校验。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm test:core`：53 个测试文件、314 项测试全部通过。
- `git diff --check`：通过。
- 交接闪帧修复复核：Edge 1280×800 逐帧探针通过；旧 Homepage 可见期间 Header 始终 `fixed`、Featured top 始终 72px，位移 0。
- 修复后 `pnpm lint`、`pnpm typecheck`、`pnpm test:core` 通过（53 files / 314 tests）。针对性 `pnpm test:smoke` 因本机缺少配置指定的 Google Chrome distribution 未能启动；同一真实 CSS/路由断言已用本机 Edge 执行通过，未将环境失败记为 smoke 通过。

## Next Task

- `T47 · 连续移动/reduced/性能验收` 已获凌巽授权并开始执行。

## Do Not Start Yet

- T47 未完成并具有独立 Evidence/Handoff 前，不进入 GATE-E。
- V16 自动化、Design Review 和本 Handoff 不代签真实手机、最终人工观感或生产发布。
