# T47-F6 · 委托首屏视觉动线收紧 Handoff

日期：2026-08-29

分支：`codex/fix-touch-adoption-pdf-export`

## Completed

- 首页自设委托幕与 `/commission` 删除“从角色设定出发”，未增加替代 slogan。
- 首页信息组直接使用营业状态、短说明和两个既有行动，媒体、路由、动效入口和 Footer 未改变。
- 用户后续复核后，首页委托在 `767px` 以下竖屏取消强制整屏最小高度与 stage 垂直居中，媒体顶部 margin 从 `4.35rem` 收为 `3.75rem`，行动组之后只保留短呼吸。
- `/commission` 在 `1023px` 以下继续选择独立竖版素材，但媒体框从完整 `9:16` 改为 `4:5`，实际高度上限为 `47.5svh`；`1024px` 及以上现有左叙事、右横图分栏保持。
- SPEC、COPY、Design、Shared Visual Language、TASKS 与 STATE 已同步，V10-F1 的旧 slogan 口径明确标记为 superseded。

## Locked Decisions

- 委托摄影继续承担角色识别和首页到内页的视觉连续性，不删除横版或竖版后台素材集合。
- 委托入口不再使用 slogan；不得以同义短句恢复第二标题层级。
- 手机和平板的摄影是受限媒体锚点，不得再次以完整 `9:16` 大图把标题、状态、说明和主行动推离第一视口。
- 首页委托竖屏使用内容高度，不恢复为通过整屏最小高度制造上下对称大留白。
- 不新增断点组件、媒体派生、字段、迁移或依赖。

## Validation

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- 本地真实浏览器复核：390×844、768×1024、1024×900、1440×900 均无水平溢出或 console error，页面中“从角色设定出发”计数为 0。
- `/commission` 主行动底部位置分别为 836、724、550、557，四档均在第一视口内；对应媒体尺寸分别为 321×401、389×486、610×558、861×558。
- 首页自设委托幕在 390、768、1024、1440 四档均保持摄影第一锚点，营业状态、说明和两个行动形成完整信息组。
- 后续竖屏间距复核：390×844 的首页委托 section 高度由 772px 收为 564px，标题线到 stage 为 12px，行动组之后约 44px；390×844 与 430×932 均无水平溢出或 console error。

## Open Issues

- 真实 iOS/Android 与王旻安/景宸最终观感仍属于 GATE-E 人工验收，当前本地浏览器不能代签。

## Regression Risks

- 后续若重新增加委托首屏文案或提高窄屏媒体高度，390×844 的主行动最先可能被推出第一视口。
- 竖版源图使用 `cover` 进入 `4:5` 画框；更换素材时仍需人工确认角色头部和主要特征未被裁掉。

## Next Task

- 由用户在真实手机和平板复核首页委托幕与 `/commission`，重点检查图片裁切、首屏主行动和自然滚动节奏。

## Do Not Start Yet

- 不启动发布、部署、生产数据修改、媒体重建或后台模型调整；这些均未获本任务授权。
