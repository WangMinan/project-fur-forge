# 状态

> **角色**：当前需求的状态机与执行入口。只记录现在有效的阶段、阻断项和下一步；历史过程见 `implementation/notes/`。
> **最后更新**：2026-08-09。
> **阶段 D 文档基线**：`93557c38c9d3d4b0586c96bef5c05d8d369fb424`；其后的提交为本次范围收敛与文档改写。

## 当前阶段

阶段 C 与阶段 C.1 已完成，`GATE-C1` 已通过。项目现进入：

> **阶段 D · P1 一期增强实施。T35–T37 工程主体已于 2026-08-08 落地，
> 并经用户两轮人工 Review。第一轮结论（返图模型解耦等六项）已实施为
> T35-F1–T35-F6；第二轮结论（2026-08-09，管理列表与公开版式收口）
> 已实施为 T35-F7。三者的工程项均已完成本地门禁，但仍待新上下文
> 独立 Review 与用户人工验收。**

## 2026-08-08 用户 Review 结论

用户在浏览器中人工核对 T35–T37 后要求以下修改。前三项改变数据事实与公开行为，`requirements/SPEC.md` 第 4、7.2、9.1、10、11、14 节已先行改写：

1. **返图与作品彻底解耦。** 返图不再必须挂在作品上，改为两级模型
   「设定 `return_characters` + 多张返图 `return_photos`」。设定有自己的
   名称与可选 `@昵称`，关联作品是可选的；老作品没上架、甚至没有作品记录，
   也可以有返图。返图能否发布不再取决于关联作品状态。
2. **一个设定可以有多张返图**，横竖混放。返图墙上每张图独立平铺，
   点击进入 `/returns/{slug}` 看该设定的全部返图，不跳作品详情。
3. **管理端返图列表需要分页**；设定编辑页支持多图上传与指定主图；
   删除无水印返图的公开预览卡（无水印时没有预览价值）。
4. **公开排序改为越新越前**（`/works`、`/adoptions`）；返图每次随机打乱，
   因此不需要排序；人工排序只保留给首页精选，且发布后仍可直接改动生效。
5. **精简过度防御与啰嗦文案。**
6. **统一公开页视觉**：图片统一圆角矩形，领养筛选条与作品筛选条一致，
   修复领养卡错位；`/admin/works` 无出厂照主图时缩略图回落设定图。

## 2026-08-09 用户 Review 第二轮结论

用户在第一轮修复后继续人工核对，提出以下修改。均已实施为 **T35-F7**：

1. **管理列表加删除按钮**：`/admin/returns` 与 `/admin/works` 一致，
   删除入口在列表行内；设定详情页不再放危险操作 card。
2. **管理列表按 `updated_at` 倒排**：`/admin/works` 与 `/admin/returns`
   都是最近改动的在前。
3. **设定详情页页头与 `/admin/works/[id]` 对齐**：返回链接、标题、
   未保存标记同一行，保存按钮在右上角。
4. **委托页横屏充分利用页宽**：正文区块在页宽内居中（与 `/service`
   一致），常见问题在宽屏分两列。
5. **开源软件声明贴许可证正文**：copyleft 依赖内嵌 GPL-3.0 全文与
   源码获取途径；MIT / Apache 依赖给出仓库地址。
6. **`/admin/returns` 表格白底**，与 `/admin/works` 一致。

## 已落地事实（2026-08-09）

返图（T35-F1–T35-F3 之后的当前模型）：

- `return_characters`（设定：名称、可选 `@昵称`、公开 slug、可选关联作品）
  + `return_photos`（一个设定多张，含 `is_primary`）；前向迁移 0024；
- 返图与作品**彻底解耦**：关联作品可选，`work_id` FK 为 `set null`，
  返图发布不取决于作品是否存在或已发布；
- `/returns` 每张返图独立平铺、按 10 分钟时间窗种子随机打乱、纯净瀑布流
  （不标名称）；`/returns/{slug}` 设定页给出圆形主图、名称、`@昵称`
  与该设定全部返图；
- 管理端设定列表（分页、查找、删除）与设定编辑（多图上传、逐张 alt、
  指定主图、逐张发布/下架）；删除设定连带删除返图并先自动下架；
- `return_photo` 私有上传、`return-display-v1` 无水印公开衍生、
  发布/下架 operation 与恢复（T35 的基础设施继续复用，未新建第二套）。

其余：

- 轻量展会掉落：四选项业务类型、`event_name` / `event_time`、
  `/adoptions` 三筛选、卡片与详情展示；前向迁移 0023；
- 公开排序：`/works` 与 `/adoptions` 按发布时间倒序，人工 `sort_order`
  只服务首页精选且发布后仍可直接改；
- 首页轮播写死开启、10 秒一张；大图顺位可在已启用状态下直接保存；
- 公开视觉：`--radius-image` 统一圆角，`PublicFilterChips` 统一筛选条，
  领养设定图固定比例外框；页脚间距收紧；
- `/licenses` 开源软件声明（写死、不进文案编辑，含 GPL-3.0 全文）；
- 管理端登录页保留 `brandOnly` 页头页脚，便于放置备案信息；
- 英文名统一为 `DITE DOG`（`PROJECT_ENGLISH_NAME`）；
- 本地门禁：lint、typecheck、unit 122/122、integration 158/158、
  production build 通过。

尚未完成，不得当作已通过：

- T35、T36、T37 与 T35-F 系列的新上下文独立 Review；
- T36、T37 的 390×844 真机手机闭环；
- 针对返图 operation 的 SIGKILL / 重复重启重放；
- 第二轮修复的用户浏览器人工验收（除登录页外尚未逐页目视确认）；
- T42。

阶段 D 范围已经确认，不再处于需求讨论状态：

1. T35：返图模型、作品关联、状态、版本与可选私有授权记录；
2. T36：返图私有上传、无水印公开衍生、管理端和独立 `/returns` 原比例瀑布流；
3. T37：复用作品与领养管理的轻量展会掉落；
4. T42：只对 T35–T37 做阶段门禁和用户验收。

T38、T40 已取消；T39 当前版本取消并转未来迭代备忘录；T41 不再单列，必要手机能力并入 T36、T37。

阶段 C 的用户验收见
[`implementation/notes/t34-c1/T34-C1-USER-ACCEPTANCE-2026-08-07.md`](./implementation/notes/t34-c1/T34-C1-USER-ACCEPTANCE-2026-08-07.md)。
阶段 D 范围决策见
[`implementation/notes/stage-d/STAGE-D-SCOPE-2026-08-07.md`](./implementation/notes/stage-d/STAGE-D-SCOPE-2026-08-07.md)。

## 阶段 C 已交付基线

以下能力继续有效，不在阶段 D 推倒重写：

- 完整作品字段、设定图、出厂照、作品发布与下架；
- 常规领养列表与统一作品详情；
- 首页 Hero、精选作品、统一业务入口和当前领养；
- 委托页、关于、官方渠道、服务条款、隐私政策与营业状态；
- 首页和委托站点展示位无水印，作品与领养展示位保留活动水印；
- 可配置居中水印 profile 与真实双 Bucket 验证；
- 分区文案 Card、稳定 FAQ ID 和分区级并发冲突处理；
- 过期上传清扫、可信代理、按主体限流和稳定业务错误；
- publication/watermark/reconcile 长任务的 lease、heartbeat、恢复与幂等；
- Node 24 镜像结构、Compose/Nginx 配置、live/ready 与运维子命令；
- 本地非 Docker 门禁、三视口浏览器检查和用户人工验收。

媒体公开规则继续以
[`requirements/MEDIA-PUBLICATION-POLICY.md`](./requirements/MEDIA-PUBLICATION-POLICY.md)
为唯一事实源。

## 阶段 D 已锁定产品事实

### 返图墙

- `/returns` 是公开站一级导航独立页面，不是作品详情内的 Tab；
- 返图与作品彻底解耦：两级模型「设定 + 多张返图」，设定有自己的名称与
  可选 `@昵称`，关联作品可选，返图发布不取决于作品状态；
- 一个设定可以有多张返图（横竖混放），每张关联一张 `return_photo` 私有原图；
- 返图墙每张图独立平铺、每次请求随机打乱，点击进入 `/returns/{slug}`；
- 返图墙是纯净瀑布流：只有图片，不标设定名称；
- 公开端采用原比例 masonry/瀑布流，图片圆角矩形；
- 返图公开图不加水印，使用独立无水印预生成衍生图；
- 私有原图、私有 Object Key、EXIF 隐私和可选授权记录不进入公开响应；
- 首版不建设返图者主页、搜索、点赞、评论、访客投稿或用户账号；
- 返图不设人工排序；
- 不建设返图或全站 30 天回收站。

### 轻量展会掉落

- 底层保持 `purpose=adoption`、`adoption_method=event_drop`；
- 管理端可以显示四个业务选项，但不新增第四种底层 purpose；
- event_drop 只要求展会名称 `event_name` 和展会时间展示文本 `event_time`；
- 复用领养状态、价格、设定图、出厂照、作品水印、发布和下架；
- `/adoptions` 提供全部/常规领养/展会掉落筛选；首页和详情显示展会名称与时间；
- 展会时间不自动改变业务状态；
- 不创建独立展会表、展会管理页、展会详情、地点、摊位、封面或历史归档。

## 当前任务状态

| 任务 | 当前状态 | 下一步 |
| --- | --- | --- |
| T35 | **模型已由 T35-F1 改写并完成** | 独立 Review |
| T36 | **已按两轮 Review 重做完成** | 手机闭环、SIGKILL 重放、独立 Review |
| T37 | **工程主体完成，待补验证与 Review** | 390×844 手机闭环、独立 Review 与用户验收 |
| T35-F1 | **工程完成** | 独立 Review |
| T35-F2 | **工程完成** | 独立 Review 与手机闭环 |
| T35-F3 | **工程完成** | 独立 Review |
| T35-F4 | **工程完成** | 独立 Review |
| T35-F5 | **工程完成** | 独立 Review |
| T35-F6 | **工程完成** | 独立 Review |
| T35-F7 | **工程完成** | 用户浏览器逐页验收与独立 Review |
| T38 | **已取消，未实施** | 不进入 T42 |
| T39 | **当前版本取消，未实施** | 未来与分享、OG、海报或二维码统一讨论 |
| T40 | **已取消，未实施** | 不进入 T42 |
| T41 | **取消独立任务** | 手机能力并入 T36、T37 |
| T42 | **等待 T35–T37** | 阶段 D 总门禁 |

未来候选见 [`planning/FUTURE-ITERATIONS.md`](./planning/FUTURE-ITERATIONS.md)。该文件不是实施授权。

## GitHub Actions 非阻断遗留

阶段 C 迁移时核对的代码基线 `3984b4f181d5a3071a119affae34c1088a53b6f9` 的 `quality` 结果为：

- `image-build`：成功；
- `checks`：lint、typecheck、unit、integration 成功后，在 Production build 失败；
- `e2e`：因依赖 `checks` 而跳过。

现有证据不足以在文档中断言具体根因。该问题已登记到阶段 E 的 T49，由届时最新 `main` 复现、修复并取得 `checks`、`image-build`、`e2e` 同一 SHA 全绿。

在 T49 完成前不得声称 GitHub Actions 已全绿；它不阻断 T35–T37。

## 下一步执行顺序

1. ~~T35 后端：返图迁移、Drizzle schema、共享契约、repository/service/route 和测试~~（已完成）；
2. ~~T36 后端：`return_photo` 上传、`return-display-v1` 无水印配方、发布恢复和公开分页~~（已完成）；
3. ~~T36 前端：独立返图管理、一级导航 `/returns` 瀑布流~~（已完成）；
4. ~~T37 前后端：展会字段、管理四选项映射、发布检查、领养筛选、首页和详情~~（已完成）；
5. ~~T35-F1 返图与作品解耦：迁移 0024、契约与后端~~（已完成）；
6. ~~T35-F2 设定列表分页与多图编辑；T35-F3 返图墙随机平铺与设定页~~（已完成）；
7. ~~T35-F4 公开排序、T35-F5 视觉统一、T35-F6 文案精简~~（已完成）；
8. ~~T35-F7 管理列表与公开版式收口（第二轮 Review）~~（已完成）；
9. 补齐 T36、T37 的 390×844 手机闭环与返图 SIGKILL / 重复重启重放；
10. 新上下文独立 Review T35、T36、T37 与 T35-F 系列（同一实现者不得代签）；
11. 用户执行浏览器逐页人工验收；
12. T42 阶段 D 总门禁（由用户勾选）；
13. T49、T50、T52 再完成发布级 CI、最终浏览器和正式环境收口。

## 当前发布边界

- 当前没有正式域名，不生成证书、不启用 HSTS、不声称完成 TLS；
- 不创建 `v*` tag，不触发 Docker Hub 正式发布，不远程部署；
- 本地测试不能替代后续正式环境演练；
- 阶段 D 可以继续开发，但 T49、T50、T52 未完成前不得宣布正式上线就绪。