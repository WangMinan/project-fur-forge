# 计划：<需求标题>

> **角色**：把 SPEC 翻译成有序、可执行的技术实现计划（对应 spec-kit `/plan`）。
> **写入时机**：阶段 2，SPEC 锁定后（即 SPEC 的 OQ 全部「已答」后）。
> **内容**：技术选型、执行顺序、关键约束；**不**含逐行代码任务（那是 `implementation/TASKS.md`）。

## 执行结论

<!-- 计划定稿时的总结；实现完成后回填实际结论。 -->

- <一句话>。

## 执行顺序

<!-- 按依赖排序的子目标；每步写要改的文件/类与约束。 -->

### 1. <子目标>

- <步骤与约束>。

### 2. <子目标>

- <步骤与约束>。

## 技术决策

<!-- 关键选型与取舍理由。 -->

- <决策>：<理由>。

## 开放问题（OQ）

> **门禁（硬性）**：本段所有 OQ 状态为「已答」前，**禁止进入阶段 3 任务**。
> **做法**：把 SPEC 翻成计划时，对技术选型、执行顺序、模块依赖、风险点追问，逐条登记。
> **编号**：阶段内从 `OQ-001` 递增。若某项继承自 SPEC 未决 OQ（理论上 SPEC 已清空，此处仅兜底），在此接续追踪。**状态**：待答 / 已答 / 已搁置（须注明原因）。
> **解答**：回答后填入「解答」列并把状态改为「已答」；解答若改变执行顺序或决策，同步更新上方相应段并注明由哪个 OQ 触发。

| 编号 | 状态 | 问题 | 影响范围 | 提问日期 | 解答 |
| --- | --- | --- | --- | --- | --- |
| OQ-001 | 待答 | <关于选型 / 顺序 / 依赖 / 风险的追问> | <模块/步骤> | YYYY-MM-DD | — |
