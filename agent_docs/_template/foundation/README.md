# 阶段 0 · 地基（Foundation）

> **角色**：固定模块边界与接口/数据库/安全口径，是后续所有阶段的地基（对应 spec-kit `/constitution`）。
> **写入时机**：需求立项后、写 SPEC 之前。
> **稳定性**：本文件一旦确定，后续阶段不得擅自偏离；任何变更必须在此登记并说明原因。

## 模块边界

<!-- 哪个改动落在哪个模块/仓库/包；哪些只读不改。 -->

- `<模块/包路径>`：<职责与本需求的改动范围>。

## 接口口径

<!-- 接口事实来源、版本、默认路径、入参/出参要点。 -->

- 事实来源：<文档/链接>。
- 默认路径：`<...>`。
- 关键约定：<...>。

## 数据库口径

<!-- 业务库/接入库、表、幂等键、禁用字段。 -->

- 库/表：`<...>`。
- 幂等键：`<字段>`。
- 禁用字段：`<字段及原因>`。

## 安全约定

<!-- 不得提交或记录到日志/文档的内容。 -->

- 不记录凭据、token、连接串、实采数据。
- <其它约定>。

## 编码与命名约定

<!-- 命名、废弃标记、日志红线、错误处理风格等。 -->

- <约定>。
