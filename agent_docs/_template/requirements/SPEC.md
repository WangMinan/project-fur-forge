# 规格说明：<需求标题>

> **角色**：定义「做什么、为什么、验收什么」，不含技术实现细节（对应 spec-kit `/specify`）。
> **写入时机**：阶段 1，foundation 锁定后。
> **原则**：只写契约与验收；技术选型留给 `planning/PLAN.md`。它是实现与评审的唯一真理来源。

## 状态

- 日期：YYYY-MM-DD
- 阶段：<规格定义中 / 已锁定待计划 / 已实现待联调>
- 本轮范围：<一句话>。

## 资料来源

| 来源 | 用途 |
| --- | --- |
| <路径/链接> | <依据：接口文档 / 模型 / 历史设计> |

## 目标

<!-- 用户故事或目标清单，用编号；说清「为什么」。 -->

1. <目标>。

## 非目标

<!-- 明确不做的事，划清边界，防止范围蔓延。 -->

- <非目标>。

## 开放问题（OQ）

> **门禁（硬性）**：本段所有 OQ 状态为「已答」前，**禁止进入阶段 2 计划**。
> **做法**：起草本文件时主动追问——目标歧义、边界用例、缺失输入、契约模糊处——逐条登记，不留口头的「大概」。
> **编号**：阶段内从 `OQ-001` 递增。**状态**：待答 / 已答 / 已搁置（须注明原因）。
> **解答**：回答后填入「解答」列并把状态改为「已答」；解答若改变契约，同步更新上方「契约」段并注明由哪个 OQ 触发。

| 编号 | 状态 | 问题 | 影响范围 | 提问日期 | 解答 |
| --- | --- | --- | --- | --- | --- |
| OQ-001 | 待答 | <关于目标 / 边界 / 契约的追问> | <契约/表/模块> | YYYY-MM-DD | — |

## 契约

### 接口契约

<!-- 端点、方法、入参、出参、边界规则。 -->

- 方法 / 路径：`<METHOD> <path>`
- 入参：<...>
- 出参：<...>
- 边界规则：<...>

### 数据模型契约

<!-- 新增/校准的模型、字段类型、反序列化规则。 -->

- `<全限定类名>`：<字段与类型约定>。

### 存储契约

<!-- 表、幂等键、写入映射、禁用字段。 -->

- 表：`<...>`，幂等键：`<字段>`。
- 写入映射：<字段 ← 来源>。

## 验收标准

### 已完成

<!-- 可静态验证的项：编译、lint、契约检查、grep 断言。 -->

- [ ] <验收项>。

### 待真实环境验证

<!-- 需要联调或真实数据才能验证的项。 -->

- [ ] <验收项>。
