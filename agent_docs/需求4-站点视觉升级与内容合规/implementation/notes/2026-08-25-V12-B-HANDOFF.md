# V12-B Handoff · About / Contact

状态：V12-B implementation、Desktop/Tablet/390/430 static evidence 与回归验证已完成。强制停止，V12-C 未开始。

## Completed

- `/about` 使用紧凑的 `ABOUT / 关于我们` masthead；工作室事实与制作范围继续来自公开内容投影，并以 editorial 主文/侧栏关系呈现。
- Masthead 与 story 的垂直节奏已按宽屏人工反馈收紧，不再用固定大高度和过量上下 padding 撑满视口。
- Contact 使用整宽浅灰 scene：Desktop 左侧为联系说明和委托申请主行动，右侧为邮箱与 QQ/QQ群目录；邮箱和二维码横排，中间使用竖线分组。
- About 内的 QQ/QQ群二维码统一为 152px 并保持横排；Mobile 仍能在 390/430 下同时显示两枚完整二维码。
- 防诈骗提示位于全部联系方式正下方，不使用横向分隔或卡片，只以左侧 2px 淡灰竖线提示；Desktop 标题/正文横排，Mobile 自然堆叠。
- 后续视觉校准将 Masthead 收紧至桌面 15rem，并把“工作室 / 制作范围”改为 Desktop 1:1 等宽列、统一正文层级；768px 以下继续使用自然单列阅读。
- 邮箱可直接显示、打开邮件客户端或复制；`/contact` 继续 301 到 `/about#contact`，Header 锚点让位保持。

## Locked Decisions

- About/Contact 继续消费既有后台内容投影；工作室事实、制作范围、邮箱、QQ、QQ群、二维码和防诈骗文案均不在前端写死业务数据。
- 联系决策顺序为申请主行动、邮箱/QQ 渠道、防诈骗提示；防诈骗属于同一 Contact scene，不拆成远离的独立页面段落。
- 不新增数据库字段、迁移、API、依赖或渠道类型；Footer 内容、布局、样式和交互未修改。
- V12-B 的 Mobile 结果完成结构安全与当前视觉证据；最终 Mobile Art Direction 仍属于 V12-G。

## Open Issues

- 无 V12-B 阻塞项。

## Regression Risks

- `ContactChannelGrid.vue` 同时用于 `/commission`；后续调整二维码尺寸或去卡片样式时必须同时检查 About 与 Commission。
- `ContactEmailActions.vue` 的 `showAddress` 为可选展示能力；不得让未启用该属性的既有调用重复显示邮箱地址。
- 防诈骗内容长度来自后台；长文案必须保持换行和容器内收，不得恢复固定高度。

## Verification

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm test:core`：53 files / 314 tests passed。
- `node scripts/capture-v12-b-evidence.mjs --verify`：通过。
- 自动审计覆盖 1440×900、2048×1080、768×1024、1023×900、1024×900、390×844、430×932：路由 200、图片解码、无横向溢出、Reduced Motion 静态、联系构图、二维码可读、44px 行动、键盘焦点、邮箱复制、单渠道、长账号、`/contact` 301 与锚点让位均通过。
- Evidence：`implementation/evidence/V12-B/before/`、`implementation/evidence/V12-B/after/`。

## Next Task

- V12-C · Commission Apply Static Redesign。新一轮开始前必须先读取 `TASKS.md` 与本 Handoff。

## Do Not Start Yet

- 本轮不开始 V12-C 页面实现，不实施 V12-D～V12-E、V12-E-F1、V12-F～V12-G、任何 Gate、V13～V16 或 T47。
- 不新增数据库、迁移、依赖、云端媒体操作、部署、提交、推送或 PR。
