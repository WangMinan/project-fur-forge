# T14–T18 前端阻塞修复（2026-08-01）

## 用户确认范围

用户明确授权 Codex 在当前 Kimi 接线分支处理六项阻塞：刷新后图片预览、原生按钮/上传排版、真实发布水印失败、favicon、作品删除、编辑页字号与发布状态横排；随后补充要求隐藏原生 file uploader，分离选择与上传动作。

## 实施结果

- 新增认证管理接口 `GET /api/admin/v1/media/assets/{assetId}/preview`，浏览器只提交 `assetId`，服务端返回 5 分钟短时签名 GET；刷新后两张真实私有原图均完成解码显示。
- 编辑器共享按钮样式提升到管理端全局作用域；文件控件隐藏，改为“选择照片”+ 文件名 +“上传出厂照”的两步控件。
- 真实 OSS 定向重跑三张资产的缺失公开 variant，当前每张均为 12 张；连续失败的直接原因是 dev server 早于 `.env` 更新启动、仍持有旧运行配置。重启后真实浏览器发布成功，作品状态为“已发布”。
- 保存出厂照成功后立即用服务端响应重建关系与 dirty 基线；上传状态卡移到上传控件旁，按钮在摘要、PUT 和校验期间显示“处理中…”。
- OSS 公开 variant 生成失败新增脱敏日志，只记录资产 ID、用途、宽度、格式、错误码、状态与 requestId，不记录私有/公开 Object Key、签名 URL 或请求正文。
- 同一发布请求对首次 OSS 冷生成短暂失败只补偿一次，替代管理员手动二次点击；第二次失败仍沿用原失败与精确清理路径。
- `brand-standard-v1` 水印图按最终输出宽度的 18% 生成嵌套缩放参数，避免顶层比例按高分辨率处理源放大；管理端复用发布检查接口，每秒显示“已生成 / 总数 / 剩余”原生进度条。
- 使用现有 `/brand/logo-full-dark.png` 声明 favicon；正式多尺寸导出仍由 EXT-01/T30 收口。
- 新增只允许未发布作品的版本化删除；先清理公开对象/variant，再删除作品，永久私有原图保留；列表复用现有影响确认对话框。
- 统一页头字号；“发布 / 草稿 / 可以发布”进入同一横排行；真实列表出现删除入口。

## 验证

- `pnpm typecheck`
- `pnpm lint`
- `pnpm test`：74 项通过
- `pnpm test:integration`：59 项通过
- `admin-media`、`admin-works` 与 `admin-publication` 定向 E2E：35 项通过
- 本轮增量复核：lint、typecheck、4 项 `media-recipe` 集成测试、13 项 `admin-media` E2E 通过；覆盖上传进度、保存后 dirty 清除与持久化。
- 本次发布修复复核：lint、typecheck、59 项集成测试、7 项 `admin-publication` E2E 通过；覆盖首次 OSS 失败后同请求成功、水印嵌套缩放参数与发布进度条。
- 真实浏览器：先下架并清理 36 张旧衍生图，再只点击一次发布；进度从 0/36 自动更新到 18/36 后完成，操作版本 16 为单条 `DONE`，三张图片各有 12 个公开 variant，页面恢复“已发布 / 发布成功”。
- 18% 最终复核：lint、typecheck、74 项单元测试、59 项集成测试、131 项 E2E、构建与生产冒烟通过。
- 真实 OSS 480 px WebP 成片：18% 水印位于所选安全角；操作版本 20 单次发布为 `DONE`，三张图片各有 12 个公开 variant，页面恢复“已发布 / 发布成功”。

2026-08-01，用户确认 T14–T18 联合验收通过；任务框已勾选，下一项为 T19。
