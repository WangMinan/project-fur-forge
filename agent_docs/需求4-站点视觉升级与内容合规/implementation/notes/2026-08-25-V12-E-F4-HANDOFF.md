# V12-E-F4 Handoff · Featured Desktop Composition Density

状态：V12-E-F4 implementation、1280/1440/1920 Desktop 与 768/390 Structural Safety screenshots、自动回归和 Design Review 已完成。重新停在 `GATE-DESKTOP`，等待凌巽人工验收；未放行前不得开始 V12-F。

## Completed

- 将 Featured 的角色名称、物种、主行动与上一项/下一项收进同一信息组；Desktop 中信息先于切换器并整体上移，形成完整左侧 editorial grouping。
- Desktop 大屏摄影从第 7 列开始并放宽至 `34rem / 36vw / 可用高度` 三重约束，摄影向中部延伸并与背景 `SELECTED WORKS` 建立更明确 overlap。
- 1280×800 继续由可用高度限制媒体，不挤压底部 wayfinding；1440×900、1920×1080 使用更大的媒体尺度减少空场。
- 按后续人工复核复用 Homepage Adoption 的 `--public-media-canvas`：外框尺寸不变，响应式内边距缩小照片；画布保持 `12px` 共享圆角，照片使用 `8px` 内圆角和真实裁切。
- Tablet/Mobile 继续保持原有“图片 → 切换器 → 角色信息”顺序，没有把 Desktop grouping 机械缩小到窄屏。

## Locked Decisions

- 不新增文案、装饰、卡片或依赖；饱满度只来自 Typography、信息组与摄影的空间关系。
- 浅灰承托只复用既有 media canvas token；不新增色值、不改 `cover` 策略、不改变媒体外框或链接语义。
- Featured 两个真实代表作品继续等权 hard cut；名称、物种、CTA、循环切换和真实 `01 / 02` 语义不变。
- V12-E-F3 的共享蓝色 primary CTA 与 V13 四条跨页 Motion 白名单保持不变。
- Hero 品牌文字、Footer、业务数据、Schema、媒体 DTO 和 Motion 均未改变。

## Open Issues

- 无 V12-E-F4 实现阻塞项。
- `GATE-DESKTOP` 的凌巽人工静态审查尚未签字，这是下一步唯一开放事项。

## Regression Risks

- 大屏媒体继续依赖可用视口高度上限；后续修改 Header 高度或 scene padding 时应回归 1280×800 和 1440×768。
- 后续 V12-F 必须独立处理 Mobile composition，不得直接复制 Desktop 的信息顺序与媒体尺度。
- V13 加入 Featured Motion 时只能在本轮冻结的终态上做 transform/opacity，不得让动画重新改变最终布局。

## Verification

- `pnpm check:fast`：通过；test groups、lint、typecheck、53 个测试文件与 314 项测试全部通过。
- `git diff --check`：通过。
- 1280×800：媒体画布 441×588、照片约 403×550，信息组 485×238，无水平溢出，底部 wayfinding 可见。
- 1440×900：媒体画布 513×684、照片约 470×641；canvas `rgb(236, 235, 242)`、外内圆角 `12px / 8px`。
- 1920×1080：媒体画布 544×725、照片约 496×677，信息组 496×243，无水平溢出。
- 768×1024 与 390×844：两个切换按钮保留、移动端 DOM 顺序不变、无水平溢出；390 灰边为 8px。
- Screenshots：`.design/screenshots/v12-e-f4-*`。

## Next Task

- `GATE-DESKTOP` · 凌巽人工全站 Desktop 静态 Art Direction 验收。

## Do Not Start Yet

- 未获凌巽本人明确放行前，不开始 V12-F、V12-G、`GATE-MOBILE`、V13～V16、T47 或 GATE-E。
- 本轮不实现 Motion，不修改其余 Homepage scene。
