# V07-F2 · Legal / Privacy / Licenses Reading System Handoff

日期：2026-08-23
范围：服务条款、隐私政策与开源软件声明的长文阅读层级、移动许可证布局和原生展开状态；不修改法律/许可证文字、生成数据、下载产物、重定向或 Footer。

## Completed

- `PublicLegalDocument` 复用现有纯文本拆段，并将“数字. 标题”提升为自动目录、语义 `h2` 与稳定章节锚点；非编号前言和全部正文原样保留。
- 服务条款与隐私政策在移动端使用两列紧凑目录、编号章节和 44rem 阅读宽度；桌面目录成为左侧 sticky 索引，正文保持单一阅读列。
- 章节标题可作为 hash 目标并让开固定 Header；目录链接保留键盘 focus-visible 与 44px 触控高度。
- 许可证列表在 1024px 以下按名称、用途、许可证单列呈现，长授权名不再压缩说明；1024px 起恢复三列扫描。
- GPL 与许可证统计继续使用原生 `details`；summary 整行可点且至少 44px，GPL 等宽正文限制高度并独立滚动，展开后不产生页面横向溢出。
- `/terms` 继续 301 到 `/service`，完整 TXT 下载地址和生成式许可证数据保持不变。

## Evidence

- 修改前：`implementation/evidence/V07-F2/before/`
- 修改后：`implementation/evidence/V07-F2/after/`
- `implementation/evidence/V07-F2/after/service-390x844.png`
- `implementation/evidence/V07-F2/after/privacy-390x844.png`
- `implementation/evidence/V07-F2/after/licenses-390x844.png`
- `implementation/evidence/V07-F2/after/licenses-details-open-390x844.png`
- `implementation/evidence/V07-F2/after/service-1440x900.png`
- `implementation/evidence/V07-F2/after/privacy-1440x900.png`
- `implementation/evidence/V07-F2/after/licenses-1440x900.png`
- `implementation/evidence/V07-F2/after/audit.json`
- `.design/screenshots/review-service-{mobile-375,tablet-768,desktop-1280}.png`
- `.design/screenshots/review-privacy-{mobile-375,tablet-768,desktop-1280}.png`
- `.design/screenshots/review-licenses-{mobile-375,tablet-768,desktop-1280}.png`

## Verification

- `node scripts/capture-v07-f2-evidence.mjs before` 保存同数据三页面、五断点基线。
- `node scripts/capture-v07-f2-evidence.mjs after` 通过：三个页面 390/430/768/1024/1440、语义标题与锚点、无水平溢出、summary 44px、键盘 focus-visible、GPL 展开、TXT 下载、`/terms` 301 与 Reduced Motion 均为 true。
- `pnpm test:groups` 通过（core 53 / smoke 1）。
- `pnpm lint` 通过。
- `pnpm typecheck` 通过。
- `pnpm test:core` 通过（53 files / 314 tests）。
- `git diff --check` 通过。

## Locked Decisions

- 法律与许可证内容仍由现有内容投影和生成文件提供；不写死章节文案，不渲染管理员 HTML。
- 编号解析只提升已有“数字. 标题”，其它纯文本保持原样；不引入 Markdown、富文本或新依赖。
- 许可证清单、GPL 原文、TXT 下载产物和 `/terms` 301 均不变。
- Footer 内容、布局、样式、响应式和交互冻结。

## Open Issues

- Header、原生 controls 和全站 focus 几何最终统一属于 V10。
- 真实手机、系统字体与全站偏好复核属于 V11、T47 和 GATE-E。

## Regression Risks

- 把 768px 许可证提前恢复三列会再次压缩长授权名称和用途说明。
- 将纯文本解析扩展为 Markdown/HTML 会扩大安全和内容契约范围。
- 移除 GPL 内部滚动边界会让展开状态占据极长页面并增加横向溢出风险。

## Next Task

`V08 · Commission Apply Complete Form States`

## Do Not Start Yet

- V08-F1～V12、T47、GATE-E
- 法律/许可证文字、生成数据、下载产物、Footer 或业务逻辑变更
