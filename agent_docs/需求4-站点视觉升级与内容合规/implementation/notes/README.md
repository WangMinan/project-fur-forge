# 实施备注

> **角色**：阶段 4 实施过程中的执行痕迹；正式契约只回写 SPEC/models/PLAN/TASKS/STATE。
> **状态**：尚未开始应用实现。

## 当前备注

- 第二轮应用代码审查基线为 `main@aa8e5b70be0913f02ceddccdc262ec6fe0769df1`；修订后的文档已以 `main@ea3ae0a1269676db8c06c28ed32a9a29f4bd7109` 合入，后者没有应用代码变更。
- 实现前先重新 fetch main；若代码已变化，必须先做影响分析，不照搬本文中的示意类型/迁移编号。
- 第一实施单元是公开/管理行动、上传与长任务进度地基；随后先做测试减负和领养排序，再做轻量内容/隐私、单条删除、第三方声明与首页视觉。
- 申请确认只增加两个未预勾选 checkbox 和服务端 literal true，不实现 intake metadata、申请 v2、确认持久字段或版本握手。
- Hero 焦点底层能力已经存在；双 CAS 与发布链保持，用户已将首版九宫格 UI 改为目标画面内拖动焦点和水平/垂直滑杆，不新建裁切系统。
- 人工清理表示“人决定何时和删哪些，工具完整执行”，不表示用手工 SQL/OSS 控制台代替删除能力。
- 当前 release workflow 把包含 FFmpeg 的镜像发布到公开 Docker Hub；按二进制分发场景生成声明和对应源码/构建证据，不能沿用“未分发”口径。
- Apple Design Skill 只作为原则来源：即时反馈、空间一致性、可中断手势、克制材料、排版和 reduced preferences；当前非手势动效不默认引入 spring 库。
- 实施 finding、命令、Actions run、截图与真实浏览器证据按日期另建文件追加；不要把临时结论沉淀在本 README。
