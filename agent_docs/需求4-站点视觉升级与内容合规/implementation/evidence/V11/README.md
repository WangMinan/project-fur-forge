# V11 Evidence

`after/` 为 V11 最终静态证据：

- Homepage Adoption：1440×900、390×844、430×932 默认项与第三项选择状态；
- `/adoptions`：三视口页面首屏与目录起点，包含放大页头、右上斜向淡色工作室标志、无上下边线的右侧搜索/联系操作组、Desktop 左媒体/右信息、Mobile 上下重排、无标签无横线三行业务值与可辨认连续 folio；
- Adoption Detail：三视口咨询行动与统一详情结构；
- `audit.json`：动态 top-3、循环控制、完整媒体、双列/单列、可领养数量、条件搜索结果、连续 folio、精简信息面板、联系入口、焦点、圆角、无横向溢出和静态无动画断言。

证据由 `node scripts/capture-v11-evidence.mjs` 重新生成；脚本通过才写入最终 `audit.json`。
