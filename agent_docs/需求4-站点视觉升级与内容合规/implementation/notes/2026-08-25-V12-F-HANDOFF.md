# V12-F Handoff · Mobile Homepage Independent Art Direction

状态：V12-F implementation、390×844 / 430×932 默认/多项/长文/边界 Evidence、自动审计、Desktop 冻结回归与 Design Review 已完成。当前停在本 Handoff；不得在同一执行单元开始 V12-G。

## Completed

- `HomeHeroCarousel.vue` 只在 Mobile 优化 scrim、supporting copy 宽度和周边留白；未修改“有点小狗工作室”的内容、字体、字号、字重、行高、字距、核心位置、对齐或首次入场逻辑。
- `FeaturedWorks.vue` 在 Mobile 提升浅灰媒体画布的摄影存在感，保留真实双项上一项/下一项、角色信息、行动与 `12px / 8px` 外内圆角，并为 controls 与信息组保留明确间距。
- `HomeBusinessEntries.vue` 在 Mobile 使用满宽媒体、稍增媒体高度并以轻微错轴和更紧凑纵向节奏组织叙事；状态、说明、申请与详情行动完整保留。
- `HomeCurrentAdoptions.vue` 保留最多三个真实 `available` 角色，将三项显式选择、循环上一项/下一项和当前 `01 / 03` 收为稳定控制行；完整 `contain` 设定图、物种、价格与两个行动均保留。
- 390×844 / 430×932 的 Hero、Featured、Commission、Adoption 默认态，Featured/Adoption 多项 next，四幕长文压力态和 Homepage 全页边界截图已重新生成。
- 最终人工反馈中发现的重复 Mobile masthead 冲突已一并修正：`/works` 与 `/adoptions` 的背景英文和右上 Logo 在 390/430 均有正间距；`AdoptionCard` 使用 16:9 完整 `contain` 媒体、灰色留边、8px 内圆角、单行名称和同排物种/价格。此项不代表 V12-G 已完成。

## Locked Decisions

- Desktop 已通过的 Art Direction 继续冻结；V12-F 的 Homepage 变更只落在 Mobile 规则。
- Hero Brand Lock 继续有效；本轮没有申请或使用 Mobile-specific brand placement/scale exception。
- Hero autoplay 仍为 4s；品牌标题只在首次进入时播放一次，换图不重启；Reduced Motion 直接进入终态；font flash 修复不得回退。
- Featured 保持真实双项手动 hard cut；V12-F 不加入 autoplay、directional transition、reverse、interrupt 或 settle。
- Homepage Adoption 继续最多展示三个真实 `available` 角色，不能恢复为单项；显式选择和上一项/下一项继续循环。
- Footer、数据模型、后台、Schema、路由、业务投影和依赖均未因 V12-F 改变。

## Open Issues

- 无 V12-F 实现阻塞项。
- V12-G 尚未开始；本轮只对 `/works`、`/adoptions` 的用户明确反馈做了局部前置修正，剩余公开页面仍需完整 Mobile Art Direction 与 Final Evidence。

## Regression Risks

- 修改 Mobile scene 高度、Header 高度或页面 padding 时，必须重新验证 390×844 / 430×932 的四幕边界和 44px controls。
- Hero 后续 Motion 修改不得改变品牌静态终态，也不得在换图时重启标题入场。
- V13 的 Featured/Adoption 动画必须以当前静态终态为落点，不能重新制造布局跳动、背景字抽动或控制器错位。
- Works/Adoptions 的装饰英文和 Logo 使用不同组件；后续字体或水印尺度变化必须同时回归 390/430，不能只修一个页面。

## Verification

- `node scripts/capture-v12-f-evidence.mjs --verify`：通过；`bothViewports`、`noHorizontalOverflow`、`fourScenesFit`、`touchTargets`、`imagesDecoded`、`heroBrandLock`、`sharedRadii`、`adoptionControlRow`、`motionFactsPreserved`、`noRuntimeErrors` 全部为 `true`。
- `pnpm check:fast`：通过；test groups、lint、typecheck、53 个测试文件与 314 项测试全部通过。
- `git diff --check`：通过。
- 1440×900 Desktop：无水平溢出；Hero 品牌字体、字号/行高终态、Featured Desktop 信息列、浅灰 canvas 与 `12px / 8px` 圆角保持。
- Mobile masthead：390px 的 Works/Adoptions 英文与 Logo 间距为 28px / 25px；430px 为 45px / 46px；四种状态横向溢出均为 0。记录：`implementation/evidence/V12-F/after/mobile-masthead-audit.json`。
- Evidence：`implementation/evidence/V12-F/after/`。

## Next Task

- `V12-G · Remaining Public Mobile Art Direction`。

## Do Not Start Yet

- 本执行单元到此停止，不开始 V12-G。
- V12-G 完成后必须停在 `GATE-MOBILE` 等待凌巽本人验收；未放行前不得开始 V13、V14、V15、V16、T47 或 GATE-E。
