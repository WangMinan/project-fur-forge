# V03 · Homepage Overall Scene Composition & Continuity Handoff

日期：2026-08-23  
范围：正式 `FeaturedWorks.vue` 与首页四幕连续性；不修改业务 DTO、代表作品资格、Footer 或 V00 原型。

## Completed

- 将 V00-F1 的 B + M3 视觉语言正式落入代表作品幕：静止 `SELECTED WORKS` 背景 Typography、Typography × Media overlap、媒体第一焦点和克制分层入场。
- 保留最终产品契约：单幕、最多两张 3:4 READY 竖版出厂照、图片分别直达详情、右侧只有一个 `/works` 行动，不显示角色名称/物种。
- 没有恢复 V00 测试用轮播、folio、页次或 Previous / Next 控制；正式页面按管理端顺序同时展示真实最多两项。
- Featured 入场使用现有 `useMotionEntrance` 与 WAAPI；媒体 42px、说明 24px、CTA 10px，背景 Typography 始终静止；Reduced Motion 直接静态终态。
- 首页四幕强度形成 Hero Quiet/Cinematic → Featured Editorial Peak → Commission Media-led Service → Adoption Display/Character → Footer Quiet Closure。

## Evidence

- `implementation/evidence/V03/featured-desktop-1440x900.png`
- `implementation/evidence/V03/featured-mobile-390x844.png`
- `implementation/evidence/V03/featured-mobile-430x932.png`
- `implementation/evidence/V03/homepage-featured-desktop-1440x900.png`
- `implementation/evidence/V03/homepage-commission-desktop-1440x900.png`
- `implementation/evidence/V03/audit.json`

## Verification

- `node scripts/capture-v03-evidence.mjs` 通过。
- 1440×900、390×844、430×932：图片真实解码，无水平溢出；背景 Typography 为 `transform: none` / `animation: none`。
- 桌面 wheel 为 Hero → Featured → Commission，反向返回 Featured；点击与键盘 Enter 均直达 `/works/{slug}?view=home-featured`。
- Reduced Motion 下媒体和背景字均处于静态终态。
- `pnpm lint`、`pnpm typecheck`、`pnpm test:core` 通过（52 files / 313 tests）。
- smoke 未执行完成：仓库配置固定使用 Chrome，本机只有 Edge；失败发生在 browser launch，页面用例未开始。等价浏览器风险面由 V03 Edge 脚本覆盖。

## Locked Decisions

- B + M3 是 Featured 的视觉峰值，不复制到其它 Scene。
- 正式 Featured 遵守单幕双图/单目录行动契约，不恢复测试轮播和角色字段。
- `SELECTED WORKS` 完全静止；竖图规则不传播到 Hero、Commission、Adoption 或目录。
- Footer 内容、布局、样式、响应式和交互保持冻结。

## Open Issues

- 当前开发数据库只有一项代表作品；组件与 DTO 上限仍为两项，双项数据由 core 契约覆盖，最终真实双项画面仍需人工数据复核。
- 768/1023/1024 跨 Scene 结构与真实移动端属于 V04 / V11。

## Regression Risks

- 将背景 Typography 加入 `data-motion-layer` 会恢复不必要的晃动。
- 恢复 V00 Previous / Next 会违反当前单幕双图契约，并与 staged wheel 争夺方向输入。
- 把 Featured 的 3:4 规则传播到其它媒体会破坏各 Scene 独立角色。

## Next Task

`V04 · Homepage Mobile / Responsive Structural Pass`

## Do Not Start Yet

- V05 Homepage Shared Design Language / Tokens / Media Rules
- V06–V12、T47、GATE-E
