# 需求5 · PR 与镜像发布交接

## 本轮授权

用户要求保留其邮件文案，只修复与新文案冲突的既有测试，不增加测试或断言；验证后将当前分支通过 PR 合入 main，并启动 release-image 出包。用户另行授权必要的公开开源软件声明同步。没有生产部署或额外真实邮件发送授权。

## 初始核对与处理

- 邮件主题改为“【有点小狗】收到新的自设委托，请及时处理”；正文去除重复声明，附件说明收短。用户删除的后台删除说明段落及 licenses 导言精简一并保留，实际删除控件及安全流程未变。
- 现有邮件测试仅检查业务字段、后台链接、附件与可靠性，不依赖被删除的文字；10 项通知测试直接通过，无需修改任何测试。已对本轮前后 tests 目录源码做摘要比较，邮件测试保持不变。完整 smoke 另发现两条旧定位过时：将保存提示限定到“处理”区域、将旧“官方联系方式”标题更新为“联系方式”；仅替换原有两条断言，未增加测试或断言。
- 同步 SPEC 的邮件主题和正文口径，不回退用户文案。
- Windows 的 test:release 在 Linux/x64 notices 平台校验处停止，不能解释为声明内容错误。已在真实 Linux/x64 环境以冻结锁文件重新生成并校验，当前公开声明只新增 Nodemailer 10.0.0（MIT-0）及对应计数。

## 验证与远程门禁

- 本轮运行邮件核心测试、check:fast 和 smoke；并发 core 的媒体超时须串行复核，不通过增加断言或放宽时限处理。
- PR 在 GitHub 上执行现有 checks；合入后 release-image 在 Linux 上执行完整 release 质量门禁、镜像构建和推送。
- 本记录为提交时的过程记录；实际 PR 状态、main SHA、工作流结果及 image-release-evidence 以 GitHub 平台现场结果为准。仅启动工作流不能称为已出包。
- 生产部署仍未执行；之后仅使用工作流产出的 repository@sha256:digest，不以 latest 作为部署身份。
