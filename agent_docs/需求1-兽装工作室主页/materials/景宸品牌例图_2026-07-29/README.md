# 景宸品牌例图与改动范围（2026-07-29）

## 证据文件

- 用户提供的仓库根目录副本：`../../../../景宸的毛毛.png`
- 原始位置：`C:\Users\wangm\Desktop\OC\景宸的毛毛.png`
- SHA-256：`021DF431AB4FFD77979375F475303E0FD29A49F94A80D41AA2E439878A61493B`
- 尺寸：`4173 × 5902 px`
- 文件体积：`23,422,039` 字节（十进制 `23.422 MB`）；高于历史 20,000,000 字节上限，但低于 2026-07-29 已确认的 30,000,000 字节产品上限

该图是景宸提供的宣传例图与业务事实证据，不自动等同于可直接上线的正式 Logo、独立作品原图或网页构图模板。

## 用户已确认事实

1. 工作室正式中文名为“有点小狗工作室”。
2. 暂用英文名为 `dite dog`，后续仍需工作室确认正式拼写与大小写。
3. 图中的“狗头 + 闪电”不是正式 Logo；正式 Logo 方向接近，但仍在回执。
4. 图中这套全装的掉落价格为人民币 `15,600` 元。图内旧文字 `5600` 不作为事实来源。
5. 网站一期公开已录入的单件领养/掉落作品人民币价格；这不等于公开委托统一价目表。
6. “纯海绵头、内置风扇、全装掉落、即买即穿”只属于这一件作品，不扩大为全工作室固定配置或服务承诺。
7. 二维码是景宸本人的 QQ 二维码，一期页面暂不使用。

## 蓝色提取

可复跑脚本：`../../../../scripts/extract-image-palette.py`

```powershell
python scripts\extract-image-palette.py "景宸的毛毛.png" `
  --clusters 12 `
  --anchor "#1e3981" `
  --anchor "#637cc7" `
  --anchor "#c9cfe7" `
  --json "agent_docs\需求1-兽装工作室主页\materials\景宸品牌例图_2026-07-29\blue-palette.json" `
  --swatch "agent_docs\需求1-兽装工作室主页\materials\景宸品牌例图_2026-07-29\blue-palette.png"
```

脚本把原图缩至 `724 × 1024` 后，对蓝色系像素进行确定性的 CIE Lab 加权聚类。蓝色掩码命中 `307,268` 个像素，占采样图 `41.4456%`。完整参数、聚类结果和人工候选色邻域见 `blue-palette.json`。

建议作为首轮生产样张候选的精确色值：

| 角色 | 候选色 | 来源与使用边界 |
| --- | --- | --- |
| 深品牌蓝 | `#293C84` | 用户候选 `#1E3981` 周围像素的邻域均值；适合深色品牌面、标题或高对比文字 |
| 行动蓝 | `#324DAF` | 海报中较饱和的蓝色聚类；与白色对比度约 `7.46:1`，可进入主行动/焦点样张 |
| 中品牌蓝 | `#6274BB` | 12 类聚类中的主要中蓝；与用户候选 `#637CC7` 接近，只用于大字、图形和非正文装饰 |
| 浅品牌蓝 | `#CED3E5` | 最大浅蓝聚类；与用户候选 `#C9CFE7` 接近，适合弱背景、边界和图片占位层 |
| 深海军蓝 | `#1D2D5A` | 海报深蓝聚类；适合遮罩或极少量反白区域，不作为大面积默认页面底色 |

这些值是从单张宣传图提取的“生产样张候选”，不是最终品牌手册。公开站仍保持白色和作品图片为主，蓝色只承担品牌识别、行动与必要层次。

## 改动范围与同步结果

### A. 已同步到 `agent_docs/` 的确定事实

- `foundation/README.md`、`requirements/SPEC.md`、`STATE.md`：正式中文名、暂用英文名、Logo 未完成边界、QQ 二维码暂不使用。
- `.design/public-site/`：已把“正式名称未定”和笼统临时配色改为上述有证据的候选色及使用比例；仍保留正式 Logo 到位后的二次校准门禁。
- T01 最小应用壳：所有访客可见的占位工作室名已改为“有点小狗工作室”，英文眉题暂用 `dite dog`；内部 npm 包名、健康检查服务名和工程代号保持不变。

### B. 已完成契约与任务同步、等待后续代码任务实施

- **公开价格**：SPEC、PLAN、TASKS 已改为仅公开适用领养/掉落作品已录入的人民币价格；未录入时隐藏整个价格区域，委托继续人工估价。
- **这件作品的 15,600 元价格**：作为单件作品人民币价格，不推导成所有全装的统一价或起步价。
- **掉落形态**：修正“展会掉落一律只有头部、后续继续制作全装”的旧泛化。至少允许某件作品是已经完成的“全装掉落 / 即买即穿”，但不增加全站制作进度模型。
- **作品属性**：已采用每件作品 0–8 条有序短属性，单条 1–24 个字符且同件作品内去重；不进入工作室通用服务范围，也不建设通用 EAV。
- **图片上限**：产品上限已确认采用 `30,000,000` 字节；阿里云 OSS 图片处理默认源图上限仍为 `20 MB`，因此新增 `EXT-02`，要求杭州目标地域配额至少 30 MB 并使用无个人信息的 20–30 MB 合成图片完成 `sys/saveas` 实测。未通过时停止 T18，不设计静默压缩工作源。

### C. 当前不改

- 不提取、描摹或上线图中的狗头闪电图形。
- 不把 QQ 二维码加入公开页面或内容模型。
- 不因正式中文名改变而重命名 npm 包、健康检查服务名、仓库目录或内部项目代号。
- 不把宣传海报的斜切构图、大字价格和重复图案直接复制成首页布局。
- `planning/prototype-v1/` 的 HTML 与截图作为历史规划验证产物保留；README 与 v5 审查记录已加事实覆盖说明，新的事实在 SPEC、PLAN、`.design` 和后续生产实现中生效。

## 2026-07-29 已锁定口径

1. 一期仅公开单件领养/掉落作品已录入的人民币价格；不公开委托统一价、起步价或区间。
2. 没有录入价格时隐藏整个价格区域，不显示“价格请咨询”。
3. 永久私有原图上限采用 `30,000,000` 字节；OSS 30 MB 图片处理配额与合成图处理证据是 T18 前的执行期外部门禁。
