# T46-F5～F7 · 代表作品资格、单幕双图与作品目录

## 最终确认口径

- 代表作品最多两件；没有 READY 竖版出厂照的作品不能被选择。
- 首页只保留一个代表作品停靠幕：桌面左侧并排两张较小 3:4 竖图，右侧为短说明与唯一 `浏览作品展示` 按钮；隐藏名称/物种，整幕保持一屏。图片仍进入各自详情。
- `/works` 继续横竖等高混排；存在出厂照时优先出厂主图，完全没有出厂照的领养作品才回落横版领养封面，封面缺失时沿既有规则回落设定图。

## 实现

- `PUBLIC_FEATURED_LIMIT` 收敛为 2；service 在新建、完整编辑、展示设置和出厂照替换路径统一执行竖版资格与数量上限，管理 UI 提前禁用不可选项并显示原因。
- 管理列表 DTO 只增加内部 `portraitStudioPhotoAssetId` 投影，不新增数据库列或迁移；公开代表作品同时过滤不符合新资格的历史标记。
- 首页删除第二停靠点和横向轨道组件；`FeaturedWorks` 只消费聚合 DTO 前两项。
- `/works` 保留原等高伸展布局；公开 snapshot 已有出厂照时继续生成 3:4 work-card，仅无出厂照领养项回落横版封面/设定图。

## 验证

- 直接相关 integration：3 文件、23/23 通过。
- `pnpm check:fast`：test groups `core=52`、`smoke=1`；lint、typecheck 与 core 52 文件/313 项通过。
- `pnpm test:smoke`：10/10 通过，包含首页两次有效 wheel 进入委托、无竖版图的管理端禁用提示、上传/发布/下架主旅程。
- `pnpm build` 与 production content guard 通过。
- 真实 Chrome：1440×900 代表作品 section 为 `top=72`、`height=828`、`bottom=900`，双图 408×544，目录按钮 bottom 位于 section 内 516px；1024×900 双图 283×377、按钮 509px。390×844、430×932、768×1024、1023×900、1024×900、1440×900 均无正向水平溢出，按钮在 section 内可见，console error 为 0。

以上证明本地实现与自动主旅程，不代签真实手机、景宸/王旻安最终视觉验收、远程 CI、部署或生产状态。
