# V12-E-F3 Handoff · Homepage CTA Consistency & Motion Scope Reconciliation

状态：V12-E-F3 implementation、Desktop/Tablet/Mobile screenshots、computed-style 审计与未来 Motion 口径校准已完成。重新停在 `GATE-DESKTOP`，等待凌巽人工验收；未放行前不得开始 V12-F。

## Completed

- 删除 `FeaturedWorks.vue` 对共享主按钮的局部黑色变量覆盖；“浏览作品展示”与 Homepage Commission 的“提交委托申请”现在都使用 `PublicAction` 的共享蓝色 primary token。
- 保留代表作品按钮原有文案、尺寸、位置、44px 目标、hover/focus/active 状态和路由，不修改次级按钮。
- 将 V13 的跨页 shared-media transition 收紧为白名单：Homepage Commission → `/commission`、Homepage Featured → 对应作品详情、`/works` → 对应作品详情、`/adoptions` → 对应领养详情。
- 明确白名单外不增加跨页平滑切换；当前 Homepage Adoption → Detail 的 shared-media transition 在 V13 收口时移除，普通路由继续直接进入终态。

## Locked Decisions

- 同一语义的 Homepage primary CTA 必须消费 `PublicAction` 共享 primary token；页面组件不得再用局部 CSS 改成另一主色。
- V13 只实现文档列出的四条跨页 shared-media transition；不做全页 crossfade，不让文字、CTA、背景 Typography 或非点击媒体参与 morph。
- Reduced Motion 与不支持 View Transition 的浏览器直接进入目标页终态；业务路由和可访问性不能依赖动画。
- Hero 品牌文字、Footer、业务数据、Schema、媒体 DTO、依赖和当前静态构图均未改变。

## Open Issues

- 无 V12-E-F3 实现阻塞项。
- `GATE-DESKTOP` 的凌巽人工静态审查尚未签字，这是下一步唯一开放事项。

## Regression Risks

- 后续组件若覆盖 `--public-action-primary-*`，会再次造成同语义主按钮颜色漂移；只有明确不同语义时才允许局部变体。
- Catalog 同时存在多张媒体；V13 必须只绑定用户实际点击的 source 与对应 detail target，避免多个卡片竞争同一 `view-transition-name`。
- V13 移除 Homepage Adoption shared transition 时不得破坏其详情路由、来源 query、滚动到顶或 Reduced Motion 终态。

## Verification

- 运行态修正前：Featured primary `rgb(17, 19, 23)`，Commission primary `rgb(50, 77, 175)`。
- 运行态修正后：两者均为 `rgb(50, 77, 175)`。
- `pnpm check:fast`：通过；test groups、lint、typecheck、53 个测试文件与 314 项测试全部通过。
- `git diff --check`：通过。
- Screenshots：`.design/screenshots/v12-e-f3-*`；包含 1280×800、768×1024、375×812 的 before/after，以及 Featured/Commission Desktop viewport 对照。

## Next Task

- `GATE-DESKTOP` · 凌巽人工全站 Desktop 静态 Art Direction 验收。

## Do Not Start Yet

- 未获凌巽本人明确放行前，不开始 V12-F、V12-G、`GATE-MOBILE`、V13～V16、T47 或 GATE-E。
- 本轮不实现新的 shared-media transition；V13 按白名单统一实现和清理。
