# T19/T20 最终工程审查与收口（2026-08-01）

> 执行日期：2026-08-02。执行角色：GPT 5.6 Sol（`ENGINEERING_PRIMARY`）。
> 文件名沿用 T19/T20 批次日期；本记录只关闭 T19/T20，不替代 T21 独立门禁。

## 收口结论

T19/T20 完成定义已满足并在 `implementation/TASKS.md` 勾选。最终工程审查同时删除了残留生产 fixture 和公开联系方式，修复了公开页服务错误未进入 HTML 错误页、首页发布任务进度不可恢复、同 repository 读取可能陈旧、测试媒体未真实解码及生产验证未先迁移空库等根因。本轮复核又修正了生产内容守卫 CLI 在未显式设置 `APP_ENV` 时跳过扫描的入口缺口；守卫现在由生产构建入口无条件执行。

T21 保持未勾选；本记录不包含 T21 或后续阶段通过结论。

## 完成定义与证据

| 检查项 | 结果与证据 |
| --- | --- |
| 首页、列表、详情核心内容在 SSR HTML | E2E 直接请求 HTML，断言轮播首项、作品卡和详情事实/图集；公开页面继续以真实 repository 投影为源。 |
| 页面错误 HTML，API 错误 JSON | 未知作品页面返回完整 HTML 404；API 404/409/422/500 保持 JSON 契约。首页、精选和列表的服务端读取失败会抛入 Nuxt HTML 错误边界。 |
| 公开页面无敏感或私有对象 | SSR/DOM/构建泄漏扫描均未发现联系人、Access Key、签名 URL、上传会话或草稿；移除 footer/mobile nav 联系信息和剩余视觉 fixture。 |
| 只用活动 profile | 公开作品、精选和首页投影按当前活动 `brand-centered-v2` identity 过滤；旧 profile variant 不满足发布或读取条件。 |
| 横竖资源独立且隐藏首屏图不下载 | `<picture>/<source>` 使用独立 16:9 与 9:16 资源；媒体源分离后用浏览器请求记录证明只请求当前方向，且两方向图片都完成真实解码。 |
| 轮播默认关闭与无障碍 | 默认不自动轮播；开启后支持显式暂停、hover/focus 暂停、键盘、触摸与 `prefers-reduced-motion` 停止自动播放。 |
| 首页发布完整性 | 启用前要求横竖两份活动 profile variant 完整；私有预览不替代公开 variant。管理页显示实际完成数量和阶段，刷新后恢复进行中的 publication operation。 |
| 发布、下架与新鲜读取 | 集成测试在同一 repository 实例中证明发布后下一次普通请求立即读到新数据，下架后作品、精选和首页投影消失；E2E 覆盖发布、失败重试、下架和清理重试。 |
| 管理 API 边界 | 管理端首页 API 覆盖登录认证、CSRF、`expectedVersion` 冲突与 `Cache-Control: no-store`。 |
| 无生产 fixture | 删除生产 `shared/fixtures/visual-home.ts`、相关组件、生成脚本和 `public/fixtures/works/`；生产内容守卫阻断全部 `/fixtures/` 引用，构建通过。 |
| 三视口、CLS、键盘、对比度 | 390×844、768×1024、1440×900 覆盖首页、列表、详情和首页管理；检查横向溢出、CLS、键盘路径、焦点、减少动效与颜色对比。 |
| 大型居中水印 | 真实 OSS 使用一张 4672×7008 竖图和一张 7008×4672 横图生成四个公开尺寸；人工打开有/无水印对照，确认眼睛、耳朵、面部轮廓、配色和姿态仍可辨识。 |

## 完整门禁

| 命令 | 结果 |
| --- | --- |
| `pnpm install --frozen-lockfile` | 通过，锁文件无漂移 |
| `pnpm db:generate` | 通过，15 张 Drizzle 表，无未提交 Schema 漂移 |
| `pnpm lint` / `pnpm typecheck` | 通过 |
| `pnpm test` | 12 个文件、69 个用例通过 |
| `pnpm test:integration` | 12 个文件、77 个用例通过 |
| `pnpm test:e2e` | 165 个用例通过；覆盖公开 SSR/错误、管理安全、发布/下架、三视口与无障碍 |
| `pnpm build` | 通过；生产内容守卫通过 |
| `pnpm verify:production` | 通过；脚本先对唯一临时空库执行迁移，再启动生产构建并精确删除临时 DB/WAL/SHM |

## 真实环境与清理

- `pnpm preflight:oss`：27 项通过，run `t10-20260802T122843Z-379c1a65`；双 Bucket、匿名私有读取失败、跨桶处理和精确对象清理通过，结果未记录凭据。
- `pnpm preflight:watermark`：10 项通过，run `gate07-20260802T122908Z-f04ba5f9`；四个水印输出尺寸、匿名公开读取、像素差异和已知对象精确清理通过，未使用 Bucket 列举，结果未记录 bucket/key/凭据。
- T19/T20 真实 OSS 纵向链：run `t19t20-20260802T123825Z-06ced050` 通过。480×640 小图被固定配方门禁拒绝且未创建发布任务；作品生成 12 个活动 profile variant 后发布，同一 repository 的下一次普通读取立即出现在详情、列表和精选；两个首页项分别生成 12 个横竖独立 variant，私有预览清单和对象在启用后清空；关联启用首页项时下架返回 409，停用后下架完成且全部公开投影消失。
- 空库迁移：唯一系统临时目录中的 `empty.db` 应用 10 个迁移；16 张表、10 条迁移记录、0 条外键违规；随后精确删除 DB/WAL/SHM 并确认不存在。
- 泄漏扫描：在 `.output` 与 `test-results` 中扫描 3 个实际已配置秘密值，命中文件为 0；未设置 `APP_ENV` 的生产内容守卫实际扫描 `.output`，fixture/placeholder 命中为 0。
- 三个真实 OSS 入口均只按本轮记录的确切 key 清理，未列举 Bucket；纵向链追踪的 7 个私有对象、剩余首页公开对象、预览对象和临时 DB/WAL/SHM 均确认不存在，证据未记录秘密值。

## 浏览器证据

证据目录为 `implementation/notes/t19-t22/t19-t20/screenshots/`：首页、作品列表、作品详情和首页管理均有 390、768、1440 三视口截图。测试媒体由独立媒体 origin 提供可解码 WebP，避免以成功 HTTP 或占位 URL 代替真实图片结果。

测试服务器仍会为 T22 以后才实现的预留导航目标输出 Vue Router 未匹配警告；该观察不改变 T19/T20 的既定范围，交由 T21 独立审查者确认边界。
