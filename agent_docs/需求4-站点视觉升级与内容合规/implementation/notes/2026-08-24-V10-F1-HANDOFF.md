# V10-F1 Handoff · Commission Composition & Chinese Wayfinding

## Completed

- Homepage Commission 改为约 65% Media / 35% Service Narrative 的非对称静态构图；摄影仍为第一锚点，“从角色设定出发”成为清晰的第二锚点。
- `/commission` 首屏改为左侧服务叙事、右侧摄影，并增加可点击的 `继续查看 / 制作范围与估价 ↓`，明确提示首屏下方还有完整内容。
- 同类公开方向引导统一为中文：`下一幕 / 代表作品`、`下一幕 / 自设委托`、`下一幕 / 设定领养`、`开始申请 / 填写委托表单 →`。
- Featured、Homepage Commission 与 `/commission` 顶栏删除无必要的右侧 register 文案；标题下规则线统一限制为最大 32rem，Mobile 保持可用宽度。
- 更新 Shared Visual Language：英文大写只保留为 scene label / metadata，必要的导航指令与目的地使用中文。
- Desktop 1440×900、Mobile 390×844、430×932 及 1280/768/375 review 证据与自动审计已生成到 `implementation/evidence/V10-F1/after/`。

## Locked Decisions

- Homepage Commission 标题继续与其他首页幕使用相同尺度，不再单独放大。
- 图片统一复用 `--radius-image`；不增加卡片、阴影、渐变或额外装饰。
- Footer 与 Hero 品牌文字终态继续冻结；Hero 一次性标题入场与 4s 自动轮播不得回退。
- 用户可见方向引导使用中文；小型英文仅为非必需 metadata。

## Open Issues

- 无 V10-F1 阻断问题。
- V11 尚未开始；Adoption Character Archive 的静态设计继续按后续独立任务处理。

## Regression Risks

- 短视口调整不得再次把 Homepage Commission 或 `/commission` 的 destination rail 推出首屏。
- 后续复用 wayfinding grammar 时，不得让英文成为理解下一步所必需的信息。
- 后续 Motion 不得被用于补偿静态层级缺失，也不得改变当前摄影/叙事主次。

## Verification

- `pnpm exec node scripts/capture-v10-f1-evidence.mjs`：通过；路由、图片解码、横向溢出、12px 图片圆角、65/35 平衡、中文 wayfinding、短标题规则线和无右侧 register 均为 `true`。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm test:core`：53 files / 314 tests 全部通过。
- `git diff --check`：通过。

## Next Task

- V11 · Adoption Character Archive Static Redesign，仅在凌巽明确确认 V10-F1 后开始。

## Do Not Start Yet

- 不开始 V11、V12、Motion、UI polish、数据模型、迁移或部署工作。
